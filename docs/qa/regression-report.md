# 发布前 QA 测试报告（回归 / Release Readiness）

- 日期：2026-09-12 · 分支 codex/v2-1-implementation（HEAD 含 QA 修复提交）
- 环境：backend :5679（node --watch，Node22）、frontend :3014（Vite dev，当前源码；**注意 3013 被旧 Vite 实例占用**）、Chrome IAB 1440×900、数据卷含历史项目
- 方法：真实启动产品 + 浏览器黑盒操作（DOM 交互 + 截图视觉核验 + 后端 API 交叉验证）；主机锁屏导致 OS 级鼠标不可用，鼠标事件维度受工具限制（已用 DOM 分发 + elementFromPoint 几何核验替代并披露）

## 一、环境准备（已披露）
1. 启动 backend/frontend dev 服务；3013 被旧实例占用，改用 3014（当前源码）。
2. QA 期间临时将默认图片/视频通道 is_default 置 0 以启用 mock 回落（原始值 id2/id5 is_default=1，**测试后已恢复**）。
3. 未预置任何业务数据；全部测试数据（项目《青城夜巡人》、人物林夏、剧本、分镜、视频、成片）由 UI 操作真实创建。

## 二、核心用户流程（端到端，全部 PASS）
新建项目（名称 trim ✓ 默认值继承 ✓）→ 粘贴三场次剧本 → 场次解析（3 场/284 字/预计 10s）→ 确认剧本（影响摘要+影响明细）→ 项目素材创建人物 → 生成候选（确认 Sheet）→ 设为当前+撤销 → 本集设定引用投影（QA-006 修复后）→ 进入分镜 → 从已确认剧本创建（3 镜）→ H3 生成（确认抽屉+四项校验）→ 分镜图候选→设为当前 → H3 过期传播（需要更新+视频禁用）→ 重新生成 H3 → 视频提交（mock ffmpeg）→ 候选→采用（1/3）→ 成片门禁（逐条阻塞）→ 豁免×2（原因必填）→ 生成成片（确认抽屉→成片 v1）→ 导出 MP4（真实文件落盘 4087B）。

## 三、发现并修复的 Bug（详见 bug-report.md）

| ID | 级别 | 问题 | 状态 |
|---|---|---|---|
| QA-001 | P3 | 确认弹窗标题"确认确认剧本"重复 | 已修+UI 验证 |
| QA-002 | P1 | 确认剧本后页面显示"空白剧本"空态（内容看似丢失） | 已修：无草稿但有已确认版本时展示已确认正文+进入设定 CTA |
| QA-003 | P1 | 项目素材列表恒为空（`_all` 未在 data() 声明，非响应式） | 已修+UI 验证（全部 1·林夏可见） |
| QA-004 | P1 | 剧集中心 `_archivedItems`/`_allItems` 同模式（已归档列表恒空） | 已修 |
| QA-006 | P1 | V2.1 原生项目人物无法进入本集设定（episode_characters 无写入路径） | 已修：引用投影按名匹配已确认剧本正文；后端测试 14/14 |
| QA-008 | P1 | 分镜图生成失败（Provider 不可达 502）UI 零反馈 | 已修：catch→notice |
| QA-013 | P2→复核 | 视频任务"卡排队中"——实为 mock ffmpeg 合成耗时较长，任务自行完成；建议 UI 提示渲染耗时 | 复核撤销，转 P3 建议 |

## 四、BLOCKED 项
- 真实 Provider 出片：图片服务(127.0.0.1:18080)与 ComfyUI(8188)在本环境未运行 → 真实通道生成无法验证（配置存在时失败路径已验证并修复反馈缺失）。
- 文件上传控件（input[type=file]）：IAB 工具不支持文件选择器，上传维度未验证。
- OS 级鼠标事件维度：主机锁屏，已用 DOM 分发替代（交互逻辑与渲染已验证，指针命中/悬停类微交互受限）。

## 五、回归
- 后端 1156 pass / 0 fail（Node22）；前端 366 pass / 0 fail；原型回归 174 pass / 0 fail；build 通过。
- 新增回归测试：`frontweb/test/v21QaRegress.test.js`（this._x 必须在 data() 声明的静态扫描）+ `v21Assets.test.js` QA-006 投影用例。

## 六、遗留（不阻塞，已登记）
- 3013 端口被旧 Vite 实例占用（建议关闭旧进程，避免用户误开旧版页面）。
- 视频 mock 任务耗时提示可优化；胶片卡短 ID 标签可读性；概览"放弃更改"按钮仅在 dirty 时渲染（设计口径）等 P3 项见 bug-report.md。
