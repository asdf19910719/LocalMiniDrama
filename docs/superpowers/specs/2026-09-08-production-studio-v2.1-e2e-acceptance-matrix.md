# Production Studio V2.1 十个功能包端到端验收矩阵

日期：2026-09-08
状态：P0 权威附件，阶段验收、一次性迁移与旧实现删除依据

## 1. 通用验收模板

每个用例必须记录：前置数据、操作步骤、API/状态变化、失败或取消结果、并发结果、新建或迁移数据、文件/数据库保护、截图或 manifest。只验证静态文案或 Toast 不算业务验收。

证据至少包括：测试命令与结果、数据库断言、关键请求/响应（脱敏）、UI 截图、输出文件 hash。任何远端付费 Provider 用例默认用 fake adapter；真实付费冒烟需单独授权。

## 2. 功能包验收

### A. 项目与剧集生命周期

| ID | 场景 | 必须验证 |
|---|---|---|
| A-P01 | 项目列表、新建/编辑、项目概览与项目设置 | 项目卡不显示四组异构阶段计数，整卡进入概览且唯一 CTA 精确恢复剧集/阶段；概览四阶段统一以“集”为项目级统计单位，不混入素材项或镜头数；`ResumePosition` 与推荐动作分离并通过 URL 恢复场次、镜头、工作区、检查区、时间位置和候选焦点；只有阻断当前位置的硬 Gate 覆盖“继续工作”，其他问题明确影响范围；概览的空项目、Loading、Load Failed、Storage Offline、Needs Attention、All Complete、Project Missing 状态行为互不混用；项目操作首层为 Popover，导出/恢复/回收站确认/高级工具按风险分层；搜索仅匹配项目名，`q/status/sort` 可通过 URL 往返，排序和全部/制作中/需要处理/已完成/已归档筛选真实改变结果；Empty、No Results、Offline、Task Paused、索引加载失败均给出正确恢复动作；新建项目名称默认空，来源卡只选中，主动作随六类来源变化且只提交一次；画幅、时长、目录使用受约束控件；新项目只能创建第 1 集，不泄漏既有项目目标；校验、创建中、名称冲突、目录不可用、空间不足、取消、失败和防重复提交均可恢复；项目设置按项目资料与画面风格、生产对象、外部 AI 顺序展示且无页面级保存；项目资料与创作上下文独立保存，Idle/Dirty/Saving/失败保稿/离线/关闭确认/草稿恢复行为正确；资料变化使协作上下文需要更新；画面风格支持预设/自定义、搜索、影响预览和创建新版本且不自动重生成；编辑/复制上下文与创建制作任务三动作分流，制作任务复用剧集来源中心；人物/场景/道具精确进入项目素材筛选；普通创作文本变化不令媒体整体 stale |
| A-P02 | 示例导入失败/成功 | 失败零残留；成功可打开、可再次导出 |
| A-P03 | 小说/章节批量导入 | 在六类来源选择器进入；预览章节、拆集建议与集号冲突；只创建剧本草稿，不自动解析资产或确认阶段；取消零写入、重试不重复 |
| A-P04 | 当前 ZIP 往返与历史 ZIP 拒绝 | 初始、校验、可导入、警告、不支持、导入中、成功、部分成功和完整回滚逐态切换真实主动作；首层显示项目内容，技术检查折叠；最终名称、目录、媒体策略、剩余与峰值空间可确认；缺失媒体为可导入警告并可查看清单；多 GB 导入进入可离页恢复的持久任务；失败报告步骤且数据库/媒体/临时文件零残留；成功后不能重复导入，历史任务不自动运行；当前归档的资产、剧集、分镜、导入来源、媒体引用对账；ZIP 与 JSON 入口分离；未知/历史版本显示检测/支持版本，零写入并返回 `ARCHIVE_VERSION_UNSUPPORTED` |
| A-P05 | 项目删除/恢复/清理 | 影响清单、软删除、恢复、显式物理清理和路径边界 |
| A-E01 | 剧集中心与单集增删改 | 紧凑项目头与列表；顶部为全部/制作中/需要处理/已完成且空项目归零；280ms 搜索按集号/标题/最近位置生效；默认集号升序且最近工作使用真实时间；状态筛选为主、阶段筛选承接概览深链；项目空与筛选空分离；每阶段按可进入/未解锁/阻塞/完成/需要更新执行 Gate；集号唯一、并发 409、可恢复归档与恢复后路由/任务正确，不提供日常物理删除 |
| A-E02 | 批量剧集导入 | 部分非法时整体拒绝；合法时顺序稳定 |
| A-E03 | 重排/改集号 | 冲突不覆盖；剧集 ID、导入来源和各阶段内容不变；最近工作 locator 随之保持有效 |
| A-E04 | 复制剧本 | 只创建新 ScriptRevision；不复制批准、资产采用关系、媒体、运行任务或外部导入 attempt |
| A-E05 | 继续制作 | 硬阻塞优先于最近位置；locator 有效时精确恢复剧集、阶段、场次/分镜、Inspector、候选或时间线位置；对象失效时回到正确阶段并提示；空白剧集打开同一六类来源选择器；所有来源先经过安全目标选择且不得默认写入 E01 |

### B. 状态、版本与 Gate

| ID | 场景 | 必须验证 |
|---|---|---|
| A-ST01 | 三层状态并存 | `approved + failed task + warning` 同时展示，不互相覆盖 |
| A-ST02 | 提交/退回/批准 | 只允许真值表转换；事件历史不可变 |
| A-ST03 | 上游变更 | 编辑草稿不 stale；批准新 revision 自动传播 stale |
| A-ST04 | 豁免 | 只有 waivable blocker；原因必填；输入变化自动过期 |
| A-ST05 | 双窗口并发 | 所有写命令旧 revision 返回 409，刷新后无数据丢失 |
| A-ST06 | 继续使用旧批准链 | stale 版本只允许低风险预览，不能绕过 blocker 进入最终交付 |

### C. Look、资产与公共库

| ID | 场景 | 必须验证 |
|---|---|---|
| A-L01 | Look 三层继承 | Shot > StoryScene > Project，删除覆盖后恢复继承 |
| A-L02 | 项目 Look 更新 | 只使继承对象 stale；旧批准媒体保留 |
| A-L03 | 系统/自定义风格目录 | 分类、搜索、选择、详情、自定义保存和项目画面风格版本创建分离；切换前展示真实引用、需要更新和保留历史，明确不会自动重新生成；历史比较和设为当前保持项目作用域 |
| A-A01 | 项目素材候选使用/本集素材/进入分镜检查 | 项目素材首屏只有人物/场景/道具/需要处理四项统计；卡片为默认且列表偏好可恢复；资料修订、媒体候选、当前图指针语义独立；生成/上传只建候选；点击候选立即更新 current、旧 current 留在历史并可撤销；变化只影响未来选择，不改已确认剧集快照；项目素材无多级批准；本集素材页头与返回上下文明确，默认只显示带缩略图的继承摘要、差异与阻塞，首次准备按真实快照状态展开完整所需项；图片比较和条件音色在页内决策且只修改本集；外部包显示候选/项目/本集三方映射，缺失任务提交前展示通道、模型、参考、数量、费用和耗时；“进入分镜”覆盖自动检查、事务快照、成功跳转、检查失败、快照失败与媒体离线恢复，不出现用户可见审批术语；对象/候选/任务/使用/问题状态可并存 |
| A-A02 | 跨集复用 | 两集可引用同一精确 asset version；项目 current 改选不影响已确认的另一集快照 |
| A-A03 | 角色状态/四视图/SD2/音色 | 人物详情展示全部状态切换、各状态当前使用项/候选、默认标记、许可、引用和删除保护；项目资产无平级声音分类；人物音色支持试听、来源区分、候选一次改选且不影响人物形象；缺少音色仅在所选音频策略明确要求时阻断 |
| A-A04 | 本地个人资产库双向操作 | 个人资产库作为 P1 跨项目复用入口，首版只收人物/场景/道具/音色；卡片必须有视觉/试听预览，搜索、类型/状态/来源筛选真实改变结果并显示数量与空态；详情展示版本、来源许可、使用位置和文件状态；库→项目先选目标，再选“使用这个版本”（默认固定引用）或“复制到项目”，成功后返回可打开的项目素材；项目→库只保存明确选中的当前版本，不带入候选/任务/剧集私有信息；本地导入覆盖预览校验、重复 hash、失败/部分成功/重试；离线对象禁止新项目使用并提供重新定位；相同 hash 冲突必须展示双方并选择使用已有、保存新版本或独立资产；被引用条目只能归档，不能物理删除 |
| A-A05 | 外部包资产匹配 | source_key/hash 自动精确匹配；同名只提示，需人工决策 |
| A-A06 | 资产历史图归档/删除 | 日常删除为可恢复归档；被 revision、Shot、任务、候选或交付引用时禁止物理删除；高级清理执行依赖与路径检查并生成报告 |
| A-A07 | 场景资产/道具 CRUD 与从图提取 | 新建、编辑、派生剧情状态、视图参考、从图提取和候选改选均可追溯；默认只展开当前状态和主视图；场景单图/四宫格为基础层，独立俯视图/全景图在高级层并能进入分镜参考绑定、H3 引用编译和走位/连续性检查；四宫格拆分不自动设为 current；“叙事场次”与“场景资产”不混用 |

### D. 剧本和结构化解析

| ID | 场景 | 必须验证 |
|---|---|---|
| A-S01 | 自动保存与冲突 | 用户层唯一显示未保存→保存中→已保存时间；800ms、Ctrl/Cmd+S、条件性浏览器恢复副本；保存失败/离线/409 禁止确认，冲突比较本机与服务端且不覆盖任一版本 |
| A-S02 | AI 剧本候选与来源入口 | 续写/选段改写异步生成候选并先展示 diff，应用后才修改草稿；无选区时禁用选段改写；多集/小说/项目复制结果逐集为 draft，不自动解析、批准或刷新下游 |
| A-S03 | 预计素材变化 | 草稿保存后后台轻量解析；确认前逐项展示新增、属性变化、删除候选、人工锁定和采用/忽略；解析失败可重试且不阻止保存草稿 |
| A-S04 | 检查并确认新剧本 | 空剧本/未保存/保存中/失败/冲突阻断；检查显示场次、字数、预计时长和具体下游数量；批准必写 stale，绝不自动删除/覆盖/重新生成媒体，成功 CTA 转为进入本集素材 |
| A-S05 | 一次性剧本迁移 | 迁移器建立 ScriptRevision；文本 hash 不变；迁移后编辑只写新事实源 |

### D2. 单集制作包与外部协作

| ID | 场景 | 必须验证 |
|---|---|---|
| A-I01 | 五步单集 JSON 导入 | `episode-package@2.1` 的必填/未知字段/引用/时码错误返回固定错误码；字符串 `"2.1"` 通过 Schema，数值 `2.1` 返回 `PACKAGE_SCHEMA_INVALID`，其他字符串版本返回 `PACKAGE_VERSION_UNSUPPORTED`；所有来源先选创建下一个可用集号或填空；预览展示文件状态、标题/梗概/剧本片段/完整剧本、场次顺序和内容问题；素材复用固定 asset/version/state/media；Provider 约束标记为生成前校验；确认汇总创建/复用/跳过和外部媒体；预览零写入；事务内重检；非空返回 `TARGET_NOT_BLANK`；零媒体任务；成功返回真实剧集 ID 并同步更新行、统计、来源与报告落点 |
| A-I02 | 外部 AI 协作任务包 | 从制作包入口的明确次动作进入；先选安全目标；生成上下文时显示时间和上下文/素材版本且过滤本地敏感路径；冻结唯一任务 attempt；ZIP 与单文件任务 JSON 使用同一 `package_id/assets_digest` 并冻结目标、上下文、结果 Schema 和资产快照；等待任务在剧集中心与任务中心持久化，支持选择结果、复制说明、重新下载和取消；回流必须匹配 package/digest/schema/project/episode/context revision；快照变化提供冻结导入、重新匹配或重建任务；摘要不符、重复 `source_key`、缺失 `character_ref` 明确阻断；既有人物新状态可确定性适配；结果回流复用原 attempt 并走同一五步检查 |
| A-I03 | 导入来源 | 剧集行与 Studio 均可进入只读审计；原始 JSON、规范化结果、匹配决策、导入报告和 source hash 分区可查看且不可变，项目资产后续改名或换版本不改写历史证据 |

### E. 分镜与 Prompt

> 2026-09-10：本节按 STORYBOARD-021 五区布局更新；布局类验收以“左检查器 / 中时段提示词 / 右结果”为口径。

| ID | 场景 | 必须验证 |
|---|---|---|
| A-B01 | Shot CRUD、导航与结构更新 | 场次选择、镜头下拉、上一镜/下一镜、底部轨道和恢复地址切换同一真实镜头；插入、删除、重排、复制、集内编号和并发冲突；“更新分镜结构”（更多菜单）显示来源剧本/费用/耗时及逐镜 diff 后真实创建新分镜版本并保留人工锁定/媒体历史；Excel/CSV/shot-package@2.1 JSON 导入经过字段映射、结构预览、资产匹配和版本差异后创建新版本，完整单集包转剧集页且任何入口都不直接覆盖 |
| A-B02 | 单/多时段 | 单时段合法；中栏时段卡的画面/动作与对白/声音可就地编辑；拆分/合并/重排真实改变数据并保持时段连续闭合、总时长不变；dirty/saving/save-failed/409 均可恢复 |
| A-B03 | 多场景/多资产与联合预检 | 引用素材 chips 与时段引用并集一致并按 `@图片N` 对应槽位；必需引用、Provider 能力、生成描述（H3 草稿）和同镜活动任务使用同一门禁四行检查，任一失败或运行中均禁用提交；Provider 不支持时给出拆分或更换模型建议 |
| A-B04 | 时长与耗时呈现 | 输出时长只显示单一字段（如 7s），15s 不作为默认值；预计耗时单行显示，页面不出现排队/处理/总耗时分段和计划/请求双时长 |
| A-H01 | PromptStyleGate | 正向块完整且一次、负向完整、快照与提交逐字一致 |
| A-H02 | H3 一等公民与校验 | 中栏 H3 词条提供独立生成动作、状态芯片与可折叠可编辑草稿；dirty→saving→valid/invalid、来源摘要与结构/引用槽位/音频语义/提示词风格四项校验可见；分镜图变化或来源变化令草稿“需要更新”，dirty/saving/invalid/stale 均阻断正式任务且不覆盖人工文本 |
| A-H03 | 人工 prompt | 上游变化不覆盖人工文本，只标 stale |
| A-B05 | 分镜图候选、图片提示词与导出 | 左栏分镜图区展示当前图与候选缩略，点击打开大图预览抽屉并可“设为当前分镜图”；分镜图提示词默认按时段与引用自动拼装、可直接编辑（标记已手工覆盖）并可恢复自动拼装；生成分镜图抽屉显示本次完整提示词；上传图片直接进入候选且不自动替换当前图；导出入口在更多菜单，内容来自当前明确结构并带状态摘要；管理镜头引用抽屉可按组移除/添加引用（本镜场景不可移除），增删即时重排 chips 并令 H3 标脏；素材预览抽屉展示本集固定版本、素材库最新版本、出现时段与换绑/跳转操作；生成视频抽屉可选择 1–3 个候选数量并按数量乘算费用 |
| A-B06 | 首尾帧衔接 | 左栏显示上一镜尾帧→本镜首帧衔接状态（已衔接/可衔接/等待上一镜完成）；衔接只在用户确认后建立，断开不删除媒体 |

### F. Provider 与生成任务

| ID | 场景 | 必须验证 |
|---|---|---|
| A-G01 | API/网页/ComfyUI 通道 | ChatGPT 网页可设全局/项目默认；按本次→项目→全局→安装解析并冻结来源；三通道共用 Generation Envelope，提交前展示对象/状态/视图、模式、画面风格、参考用途、Prompt、输出数、任务数、费用、引用与格式能力及降级结论；ChatGPT 环境检查零生成费用；提交先创建可恢复任务，成功结果只追加候选 |
| A-G02 | 批次、任务页与耗时 | 独立任务页按项目/剧集/类型/通道筛选；`canPause/canCancel/canResume/canSkip/canReorder` 分别驱动动作；重排只允许同一 scheduler lane 的 queued 任务，提交外部 Provider 后不可重排；输出视频时长与任务耗时分离，只有可信来源才显示百分比 |
| A-G03 | ChatGPT 网页会话幂等与恢复 | 登录/桥接/捕获异常不改已保存默认值；可修复或仅本次改用 API/ComfyUI；浏览器关闭后恢复原 attempt，重复回填去重和结果重绑，同一命令不重复提交 |
| A-G04 | 后台迟到结果 | 保存为候选并标旧输入，不覆盖 adopted/approved |
| A-G05 | 未知状态 | 保留 raw status，用户可查看和安全刷新 |
| A-C01 | AI 配置基础 | Provider 配置 CRUD 与连接测试；测试不创建付费生成任务；无密钥泄漏 |
| A-C02 | 配置运维 | 导入导出、厂商锁定、批量换 Key 和并发配置往返一致 |
| A-C03 | 业务模型映射 | 模型映射和提示词覆盖可预览、版本化；项目快照不包含密钥 |

### G. 视频候选与连续性

| ID | 场景 | 必须验证 |
|---|---|---|
| A-V01 | 分镜视频提交、主播放器、候选采用与短片复核 | 分镜提交正式任务前 H3 草稿必须有效，费用确认抽屉明确输出时长、预计耗时和费用；提交后禁用重复付费操作且只追加候选；候选必须成功加载到右栏主播放器才能“用于本镜”，采用/撤销在候选卡、镜头轨与成片准备度使用同一指针。改选当前分镜图令 H3 草稿“需要更新”、旧视频标记“基于旧分镜图”并退出完成计数，但不删除历史。短片读取同一候选组，已有 Timeline clip 时必须明确选择替换或仅改默认候选 |
| A-V02 | Video Gate 与软进入成片 | 无语义自动选片；`进入成片审核（x/y）` 前显示已采用/有候选未采用或需确认旧结果/缺失/失败摘要并允许提前审片，开始合片仍要求每个必需镜头人工采用或明确可豁免跳过 |
| A-V03 | 外部网页回填 | session/attempt/result/rebind 完整且可恢复 |
| A-V04 | 首尾帧衔接与生成历史 | 左栏首尾帧衔接确认后才建立下镜依赖，断开不删媒体；右栏生成历史保留每次任务的提交/完成时间与状态，失败任务保留输入快照并按原输入重试创建新任务；取消进入 cancel-requested 且记录保留 |
| A-V05 | Provider 降级 | 更换模型或拆分分镜都需要用户确认，不丢引用 |

### H. 音频体系

> 2026-09-10：本节按 CUT-002 短片页重设计更新——短片页的声音能力收敛为合片设置（保留原声 + 整集 BGM + 旁白 TTS）；逐段归属矩阵移入音频设置/分镜上游，不再在短片页裁决。

| ID | 场景 | 必须验证 |
|---|---|---|
| A-AU01 | BGM 策略 | 合片设置提供不使用/整集 BGM；选择与合成输出一致；分镜级 BGM 策略（per_segment）在上游设置并随镜头继承 |
| A-AU02 | 对白/旁白所有权 | 合成保留镜头原声；旁白 TTS 与 BGM 混音不覆盖原声；上游归属冲突在合片设置显示提示并引导回上游处理，不在短片页逐段裁决 |
| A-AU03 | 参考音色 | 版本、授权、语言、角色绑定和缺失降级明确 |
| A-AU04 | 混音参数 | gain/ducking/fade/loudness 收进音频设置高级项（P2），默认使用安全预设 |
| A-AU05 | 成片后音频修改 | 镜头或设置变化后生成新成片版本；旧版本保留可回看 |

### I. 合成与交付

> 2026-09-10：本节按 CUT-002 更新——多轨时间线与 Picture Lock 术语从短片页移除，后期链内部执行；交付语义并入"成片版本"。

| ID | 场景 | 必须验证 |
|---|---|---|
| A-T01 | 镜头时间线（只读审片轨） | 平铺全部镜头、状态着色（已完成/生成中/失败/未生成/需确认）、场次筛选、点击切镜与上一镜/下一镜一致；**不提供**排序/裁剪/变速/转场/多轨编辑——结构调整回分镜页 |
| A-T02 | 生成成片门禁 | 全部镜头用于本镜或有效豁免、无基于旧分镜图的未确认镜头、无运行中任务；未通过时按钮禁用并逐条展示；可通过软进入提前审片 |
| A-T03 | 合成任务 | 确认抽屉展示四项设置（BGM/旁白/字幕烧录/超分）后提交；任务可取消（cancel-requested 保留记录）；内部按基础合片→统一规格→项目风格→可选超分→字幕→混音→编码执行，UI 只显示进度与终态 |
| A-T04 | 合成失败恢复 | 失败保留镜头、候选与设置；按原设置重试创建新任务；中间结果与成片历史始终保留 |
| A-T05 | 导出 | 默认输出 MP4，SRT 为可选项；成片结果条内联导出；无成片或合成未完成时导出不可用 |
| A-T06 | 成片版本 | 每次合成生成新版本（vN），历史可回看且互不覆盖；页面不展示 Picture/Post/Delivery 谱系 |

### J. 全局工具、画布、迁移和数据保护

| ID | 场景 | 必须验证 |
|---|---|---|
| A-M01 | 媒体库 | 项目/对象/类型筛选、使用位置、重定位、删除保护 |
| A-F01 | 自由创作 | 从“更多工具”可进入但不出现在默认 Rail；只保留图片/视频两个 Recipe；点击后先进入配置 Drawer，填写 Prompt、参考素材、画幅/分辨率、Provider/模型及视频时长/首尾帧，再经过能力、引用、费用和耗时预检后确认提交；统一 Generation Job 经 queued/running 可恢复，取消遵循 cancel_requested/unknown；成功结果先为“尚未归档”，提供真实预览、任务/Attempt、实际费用和下载、个人资产库、项目资产、指定剧集/场次/镜头候选、指定剧集时间线五类去向；关闭时必须保存或明确放弃；失败保留输入快照并以新 attempt 重试，首发不做批量生成、不声称音频工具已迁移 |
| A-X01 | 高级画布 | 只从阶段内高级入口进入且恢复当前上下文；旧 Canvas 路由不存在；标准/画布写同一 command API |
| A-X02 | 节点删除/重跑 | 删除关系与删除实体文案不同；已批准/引用对象受保护 |
| A-J01 | 独立任务页 | 多种任务按项目/剧集/类型/通道筛选，显示各自能力动作、跳转、失败详情和恢复；技术状态默认折叠 |
| A-J02 | waiting_external | 登录、网页操作、文件回填和人工确认都显示需要处理；关闭页面不丢 attempt，恢复不重复提交 |
| A-D01 | 删除恢复 | 项目/剧集/资产/候选/媒体/审计按数据保护矩阵执行 |
| A-MG01 | 一次性迁移对账 | 迁移前后项目/剧集/资产/分镜/媒体计数、引用和文件 hash 无未解释差异 |
| A-MG02 | 迁移失败恢复 | 任一迁移校验失败时事务回滚并恢复备份；不进入半迁移运行态 |
| A-MG03 | 旧实现删除 | FilmList/DramaDetail/FilmCreate/旧 Canvas/旧写 API/旧格式生成器、feature flag 和运行期双写均不存在 |
| A-MG04 | journal 与升级 UX | 重启检测 PRECHECK/BACKED_UP/MIGRATING/VERIFYING/COMMITTED/FAILED；空间不足零写入；失败界面和离线恢复命令可用 |

## 3. 发布级业务旅程

除逐功能验收外，发布前必须用真实本地数据库跑通以下完整旅程；fake Provider 可替代付费生成，但不得省略任务、候选、采用和 Gate 状态：

1. 空白项目→手工剧本→资产→分镜→视频→时间线→Picture Lock→后期→交付。
2. 从想法 AI 生成剧本→关键节点确认→最终成片。
3. 小说/长文本分章→多集草稿→任选一集完成交付。
4. 外部 AI 任务 ZIP/任务 JSON→外部结果→五步导入→继续制作。
5. 直接 V2.1 单集 JSON→新建或空白剧集→多场次/多时段分镜。
6. 已有视频→媒体库登记→创建项目/剧集来源→进入可编辑时间线，不伪造剧本确认或本集资产集合快照。
7. 将 ChatGPT 网页设为全局默认→零费用环境检查→人物/场景/道具/分镜生图→登录或桥接异常时恢复同一会话或仅本次改用 API→结果进入候选→点击候选才更新当前使用项。
8. H3 视频生成→结果回填→候选改选→连续性锚点→下镜生成。
9. 同一剧集在标准模式与高级画布之间往返，数据、版本和命令结果一致。
10. 自由创作→媒体库→项目资产或分镜候选，来源可追溯。
11. 应用重启→运行中/未知/已完成任务恢复，不重复提交付费请求。
12. 现有本地库备份→一次性迁移→对账；注入失败时完整恢复。
13. Picture Lock 后只改字幕/音频产生新 PostRevision；改镜头时长必须回到 Timeline。
14. 最终交付可从 manifest 追溯到剧本、Look、资产、Shot、候选、Timeline、Picture 和 Post 版本。

## 4. 阶段放行门槛

| 阶段 | 必须通过 |
|---|---|
| Phase 1 | PromptStyleGate、Provider capability、reference preflight、A-ST01～06、A-S05、A-MG01～02/04 仅在数据库副本与故障注入环境、Node 22 全量基线 |
| Phase 2 | A-P01～04、A-E01～05、A-I01～03；六种剧集来源；导入零媒体任务 |
| Phase 3 | A-S01～04、A-L01～03、A-A01～07、A-G01～03 的图片部分、A-C01～03 |
| Phase 4 | A-B01～06、A-H01～03、目标视频 Recipe preflight、A-X01～02；不得创建正式 VideoCandidate |
| Phase 5 | A-G01～05 的视频部分、A-V01～05、A-AU01～05、A-T01～06、A-M01、A-F01、A-J01～02、A-D01 |
| Phase 6 | 在正式数据库执行 A-MG01～02/04；A-P05、A-MG03、14 条发布级业务旅程和旧实现静态/运行时缺席检查 |

任何阶段只可声明自己覆盖的用例；通过模型测试或静态源码测试不能替代真实 API、数据库和 UI 闭环验收。
