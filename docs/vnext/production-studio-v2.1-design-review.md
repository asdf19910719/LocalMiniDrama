# Production Studio V2.1 全量产品、交互与制作链评审

> 首次评审：2026-09-09
> 本次复核修订：2026-09-09
> 评审对象：V2.1 架构母稿、P0 附件、统一原型、逐页决策记录、交互与 UI 规格、52 项迁移矩阵和 78 项 E2E 矩阵。
> 边界：本报告评审的是产品规格和交互原型，不代表业务代码、数据库迁移、Provider、FFmpeg 或真实媒体链路已经实现。

## 1. 复核结论

V2.1 的产品方向和领域模型正确。最新个人创作者收敛建议中关于默认界面复杂度、来源分组、本集素材重复劳动、开始合片自然触发和技术术语下沉的判断成立，现已同步到统一原型。**个人创作者规格与原型执行关口通过，可以重写产品代码实施计划；产品代码、数据库迁移和真实 Provider 链仍未开发。**

原报告关于原型占位、关键接缝、Cut 门禁、基础合片母版和超分决策持久化的判断，在当时状态下成立并已修复。后来个人版评审指出的三项具体问题——来源卡共同落到占位状态、资产冲突按钮隐式新建、已导出状态仍显示超分未决定/编码等待——也成立，已加入模型合同和自动测试。最终浏览器巡检还发现并修正了“顶部 9/9 已采用、逐镜却仍显示未采用”的同类终态矛盾。评审中要求保留运行期 V1/V2 双轨、降低 ChatGPT 网页优先级的建议仍与已确认方案冲突，不采纳。

| 原评审结论 | 复核判定 | 当前处理 |
|---|---|---|
| 产品方向正确，Local Provider、H3、精确引用和 Revision 模型是差异化 | 正确 | 保留 |
| 17 个注册页面只有 7 个完整、10 个占位 | 当时正确，现已过时 | 当前为 18/18 页面级原型（新增独立任务页）；52/52 能力有稳定覆盖映射 |
| 外部 AI 导入、剧本确认、剧集资产快照缺交互接缝 | 当时正确，现已修复 | 六来源、五步导入、剧本 revision、资产 snapshot/Gate 已可交互展示 |
| Cut `6/9` 仍可 Picture Lock、编码等待仍可导出 | 正确且属于 P0 | 已禁用对应动作并显示 blocker 与恢复路径 |
| 需要基础合片母版并持久化超分跳过/成功/回退 | 正确 | 已加入 Base Composite、Post Revision 决策和 lineage |
| Phase 6 一次迁移/切路由/删旧实现是 Big Bang P0 冲突 | 不正确 | 当前权威决定正是 Phase 1～5 内部验证、Phase 6 一次性迁移与切换；安全来自备份、journal、事务、冻结、对账和恢复，不来自运行期双轨 |
| 必须保留 V1/V2 Feature Flag、Adapter、双写和旧路由回退 | 不正确 | 与已确认“V2.1 直接替换、无运行期双轨”冲突，不采纳 |
| 快速创作和 Canvas 不应与核心路径竞争 | 正确，按“下沉而非删除”吸收 | 自由创作进入更多工具；高级画布只从阶段内进入；两者复用统一数据和任务 |
| ChatGPT 网页自动化属于 Non-essential | 不正确 | 提升为 Product Core 一级可选生图通道，可设全局/项目默认，并接入环境检测与任务恢复 |
| 完整 2D、完整 3D、视频重绘不应伪装为 V2.1 已完成产品 | 正确 | 继续排除；高级画布只提供共享领域模型的高级投影 |

## 2. 当前权威口径

当文档出现冲突时，依次采用：

1. 用户最新明确确认的逐页决策；
2. [V2.1 架构母稿](../superpowers/specs/2026-09-05-production-studio-v2-design.md)及其 P0 附件；
3. [逐页交互评审决策记录](../superpowers/specs/2026-09-08-production-studio-v2.1-page-review-decision-log.md)；
4. [交互与 UI 规格](../research/localminidrama-product-baseline/production-studio-v2.1-interaction-ui-spec.md)；
5. `docs/vnext/*` 中未同步的新旧范围研究。

迁移权威方案为：Phase 1～5 只进行内部开发、自动测试和影子验证，不接管正式路由；Phase 6 冻结任务，创建备份和 migration journal，在事务中迁移并对账，统一切换 canonical route，删除旧页面与旧 API 实现。V2.1 不保留运行期 V1/V2 feature flag、双写、旧页面回退或旧 API 兼容层。断电、迁移失败和对账失败通过 journal、事务回滚、备份恢复与人工恢复流程处理。

这里仍可存在 Provider Adapter、外部 AI 确定性 Adapter 或导入格式 Adapter；它们是领域边界，不是 V1/V2 运行期兼容层，不能混为一谈。

当前个人版产品结构固定为：全局“项目 / 资产库 / 任务 / 设置”，项目内“概览 / 剧集 / 项目素材 / 项目设置”，单集内“剧本 / 本集素材 / 分镜 / 成片”。自由创作保留在“更多工具”，高级画布只从阶段内进入。ChatGPT 网页是可设默认的 Product Core 生图通道；环境检测和任务恢复属于核心闭环。Revision、Gate、Picture Lock、hash 与 runtime owner 仍存在于底层，但默认 UI 只使用“草稿、已确认、需要更新、候选、当前使用、开始合片、成片版本”等词。

## 3. 产品与 RunningHub 对比

V2.1 应继续借鉴 RunningHub 已验证的 Studio Shell、项目—剧集—阶段心智、分镜逐镜生成、候选比较、批量预检、成本靠近提交、整集审核与时间线；不复制云账户、钱包、社区、平台发布、具体品牌视觉和模型价格硬编码。

相对参考产品，V2.1 的关键优势是：

- 本地 SQLite、文件、FFmpeg 与可替换 Provider；
- API、ComfyUI、ChatGPT 网页和外部 AI 协作并存；
- Script、Asset Snapshot、Shot、Picture、Post、Delivery 多层 Revision；
- 人物/状态、场景/剧情状态/视图、道具/状态的精确引用；
- H3 可编辑草稿、fingerprint、Reference Manifest 和声音语义校验；
- Candidate 与 current/adopted 分离，历史不因改选被覆盖；
- Picture Lock、基础合片、超分回退与交付 lineage；
- 本地数据完整性、媒体重定位、迁移 journal 和 dry-run 清理。

仍需在产品开发阶段证明而不能由原型证明的能力包括：真实 Provider capability、任务恢复、文件 hash、事务导入、媒体引用保护、FFmpeg 基础合片/编码、Post/Delivery artifact 持久化和正式迁移恢复。

## 4. 18 个注册页面复核

| 页面 | 判定 | 已明确的页面职责与关键交互 |
|---|---|---|
| 项目列表 `projects` | 通过 | 卡片已移除四组异构阶段计数，只显示上次工作、当前阶段和健康摘要；整卡进入概览，唯一 CTA 精确恢复；搜索/排序/五类筛选真实生效并写入 URL；Empty、Task Paused、索引失败与紧凑列表均已补齐 |
| 新建项目 `project-new` | 通过 | 来源卡只选中，用户通过随来源变化的主动作一次提交；名称、画幅、时长和目录使用正确控件；新项目来源固定第 1 集；校验、创建中、冲突、空间不足、取消和失败恢复均已覆盖，内部项目壳不暴露为用户步骤 |
| 项目归档导入 `project-import` | 通过 | 已补齐选择、校验确认和结果三阶段；主动作逐态真实切换；缺失媒体为可导入警告；名称、目录、媒体策略、剩余/峰值空间可确认；多 GB 导入进入持久任务；成功/部分成功、历史任务只读与完整回滚均有交互 |
| 项目概览 `project-overview` | 通过 | 四阶段统一按“集”聚合；`ResumePosition` 可精确恢复场次/镜头/工作区/候选，硬 Gate 才覆盖继续动作；下一步与待处理首屏并列，风格摘要和项目操作使用个人用户语言；空、加载失败、存储离线、需要处理、全部完成和项目不存在均有独立状态；不复制剧集列表或活动流 |
| 项目设置 `project-bible` | 通过 | 项目资料与创作上下文分离；基础画面风格具备选择、影响预览和版本流程；生产对象只做问题摘要；外部 AI 的编辑、复制与制作任务分流；不是通用图片/视频 Prompt 来源 |
| 项目剧集 `project-episodes` | 通过 | 紧凑项目头与列表、状态/阶段双层筛选、真实时间排序、逐集 Gate、精确恢复、六来源统一目标选择、持久外部 AI 任务、增强五步导入、成功回写、来源审计与归档恢复 |
| 项目素材 `project-assets` | 通过 | 首屏已收敛为四项统计和简化筛选；资料修订/候选图/当前图语义分离；三通道共用生成前检查与异步任务；真实多选、撤销、快照边界、渐进详情和异常恢复均有原型 |
| 剧本 `studio-script` | 通过 | 唯一保存状态与动态 CTA；真实空态/冲突；AI 候选先比较后应用；场次级 Diff 与历史复制；预计素材变化、具体下游影响和确认状态转换均有完整原型 |
| 本集素材 `studio-assets` | 通过 | 默认只展示带缩略图的继承摘要、差异、缺失、失效引用和阻塞；图片与音色在页内抽屉决策，外部包显示三方映射；“进入分镜”自动完成检查和不可变 snapshot 事务并覆盖完整失败恢复 |
| 分镜 `studio-storyboard` | 通过（2026-09-10 按 STORYBOARD-021 重设计） | 布局重排为 RunningHub 式五区：左栏单列镜头检查器（角色含人物状态/场景/道具/首尾帧衔接、底部分镜图候选区）、中栏时段提示词工作台（引用 @图片N chips、时段编辑器、分镜图提示词、H3 一等公民词条、用 H3 生成视频生成条）、右栏视频专属大预览（候选条、用于本镜、生成历史抽屉）、底部镜头轨；上一镜/下一镜与镜头轨真切换、时段真编辑、唯一采用状态、图像变更失效传播、联合生成门禁四行检查、任务取消/重试与成片前摘要均可交互；管理镜头引用为真实增删抽屉（场景不可移除，增删令 H3 标脏）、素材行预览含版本/出现时段/换绑操作、生成视频可选 1–3 个候选数量并按数量乘算费用；结构更新/导入/导出/批量/高级画布收进更多菜单；站位图/动作预演/连续性面板/Recipe/Manifest/双重时长/三段耗时已移除 |
| 短片 `studio-cut` | 通过（2026-09-10 按 CUT-002 重设计） | 收敛为“审片 + 合片”单屏工作台：大播放器（逐镜审看 + 连续播放 + 来源标注 + 回分镜修复）、右栏成片设置（整集 BGM/旁白 TTS/字幕烧录/超分）、成片结果条（合成进度可取消/成片版本与历史/内联导出 MP4·SRT）、底部镜头时间线（复用分镜轨，只读消费采用结果）；生成成片按 x/y 硬门禁禁用并可软进入审片；多轨时间线/声音归属矩阵/字幕编辑器/七步后期链/Picture Lock 三层版本已移除，后期在产品内部执行 |
| 任务 `tasks` | 通过 | 项目/剧集/类型/通道筛选，可信进度，ChatGPT 环境恢复，技术详情折叠 |
| 个人资产库 `library` | 通过（已按单页复审收敛） | P1 跨项目复用入口；预览优先卡片、真实搜索/类型/状态/来源筛选、详情抽屉、用于项目向导、本地导入、项目保存、hash 冲突、对象级离线恢复、归档保护；不扩展为商城或全量媒体管理器 |
| 自由创作 `quick-create` | 通过（已按单页复审收敛） | 入口下沉到“更多工具”；图片/视频先配置、预检并确认费用/耗时，再创建可恢复 canonical job；结果先为“尚未归档”，支持真实预览、五类明确去向、目标上下文、失败/离线/取消/未知恢复；首发不做批量生成，不参与四阶段 Gate，产品开发不得复用旧孤岛数据链 |
| 高级画布 `canvas` | 有条件通过 | 共享命令与领域数据的高级投影；布局是视图状态；完整 2D/3D/重绘不在范围 |
| AI 配置 `settings-ai` | 通过 | 分层配置、密钥脱敏、用途映射、能力探测、导入冲突、网页会话/重绑 |
| 常规设置 `settings-general` | 通过 | 日常路径与创作默认值；路径离线和保存冲突；修复工具不混入本页 |
| 高级数据工具 `settings-data` | 通过 | 完整性、媒体重定位、迁移/恢复记录、受控物理清理；不占项目一级导航 |

“有条件通过”不表示还缺页面，而是实施时必须守住共享领域模型和范围边界。

## 5. App Shell 与通用交互复核

### 5.1 已通过

- 产品 Rail 与原型评审工具已分离；覆盖导航器、场景切换只属于原型。
- 项目详情只有概览、项目设定、剧集、资产四个一级分区；备份/恢复/删除在项目操作，高级修复在设置。
- 路由变化会关闭 route-scoped 临时覆盖层，避免确认层跨页面残留。
- 卡片、阶段格、待处理项、任务项都携带稳定目标，不复用模糊“最近位置”。
- Detail Drawer、Generation Sheet、Inspector 和高风险确认的职责已分离。
- Initial、Empty、Loading、Processing、Success、Error、Disabled、Partial、Stale、Unknown、Waiting External 均有可查看状态。
- 状态文案、图标和禁用原因共同表达，不只依赖颜色。

### 5.2 实施阶段仍需统一的横切合同

- 焦点回退、键盘路径、小屏全屏抽屉和拖拽替代操作需要在组件实现时统一验证；
- `dirty/saving/saved/save_failed/conflict` 应由同一保存状态组件表达；
- 多选范围、跨筛选全选、费用确认和部分成功应复用统一 Batch Preflight；
- 所有危险动作必须显示影响对象、写入边界、失败后的保留内容和唯一恢复动作；
- 所有百分比都必须带可信来源；无 Provider 进度时不得用前端计时伪造。

## 6. 完整制作链复核

```text
项目/空白剧集
  → 六类来源之一
  → 剧本草稿与 approved Script Revision
  → 本集设定选择与不可变 Asset Snapshot
  → 分镜/时段、图片、H3/Prompt、Reference Manifest
  → 逐镜正式视频任务 → Candidate → 用户采用
  → 短片镜头审核与 Timeline Revision
  → Picture Lock
  → Base Composite（基础合片母版）
  → Post Revision（技术匹配/Look/可选超分/字幕/混音）
  → 编码校验
  → Delivery Revision + manifest/hash
```

### 6.1 外部 AI 接缝

外部 AI 协作从项目设定或制作包入口的明确次动作进入：先通过统一目标选择器绑定下一可用集号或空白集，再生成项目上下文和带 `package_id/assets_digest` 的任务包。上下文明确生成时间、项目与素材版本，过滤无关本地路径。用户在外部会话创作期间，等待 attempt 同时持久化到剧集中心和任务中心；结果必须匹配任务包、素材摘要、Schema、项目、剧集和上下文版本后才能进入统一五步导入。导入逐项固定精确素材版本，以事务写入结构化草稿并回写真实剧集行与报告，不自动生成图片、视频或音频，也不产生远端费用。

### 6.2 图片和资产接缝

项目素材负责人物状态、场景状态/视图、道具状态及图片/音色候选。资料修订、媒体候选和当前图指针分别建模；生成或上传只追加候选，用户点击候选即设为该精确组合的 current，旧 current 留在历史并可撤销。项目 current 变化只影响未来选择，不改已确认剧集快照。API、ChatGPT 网页、ComfyUI 共用 Generation Envelope 和可恢复异步任务。剧集设定只选择本集使用项，不复制项目素材编辑器；进入分镜时固定精确版本与 hash。

### 6.3 分镜和视频接缝

（2026-09-10 按 STORYBOARD-021 更新）分镜阶段承担首次正式逐镜视频生成。页面为五区布局：左栏镜头检查器（引用管理与预览、分镜图候选）、中栏时段提示词工作台（时段编辑、分镜图提示词、H3 一等公民词条与生成条）、右栏视频专属大预览（候选条与生成历史）、底部镜头轨。H3 草稿编辑与校验直接在中栏展示，Reference Manifest、Provider capability 与技术快照不再出现在页面上。必需引用、Provider 能力、H3 草稿和同镜活动任务在提交前以四行联合检查统一预检；生成数量可选 1–3 个候选（默认 1，费用乘算、任务并行、取消/重试逐任务执行）；生成成功只追加候选。候选必须先成功载入主播放器，用户明确“用于本镜”后才改变唯一采用指针；改选当前分镜图或增删镜头引用令 H3 草稿“需要更新”、旧视频显示“基于旧分镜图”并退出完成计数。短片读取同一事实源，允许在候选未齐时提前审片，但开始合片仍执行完整 Video Gate。

### 6.4 Gate 修正

- `6/9` adopted、存在运行中/失败的必需镜头时，Picture Lock 为禁用状态并列 blocker；
- Timeline clip 固定 candidate id，改 adopted 时必须选择是否替换时间线；
- Picture Lock 后任何画面修改派生新 revision；
- 编码校验为 waiting/failed 时导出禁用；
- Post 失败不删除 Base Composite；超分 skip/success/fallback 决策写入 Post Revision；
- 交付显示 Picture → Base Composite → Post → Delivery 的完整 lineage。

## 7. 52 项功能与 78 项 E2E

统一原型使用单一覆盖索引映射迁移矩阵中的 52 个唯一能力编号到 18 个注册页面，每项标记 `prototypeStatus=complete` 与 `productCodeStatus=not-developed`。新增的第 18 页是独立任务中心，不增加迁移能力编号。高风险流程提供完整交互；普通功能至少提供入口、前置、正常/异常状态、恢复动作和成功落点。

52 项功能覆盖与 78 项 E2E 不是同一计数：一个页面可承载多个能力，一个能力可对应多个验收项；迁移、事务、并发、hash 和断电恢复等规则不应被伪造成独立 UI 页面。进入实施计划前应保持 52 个能力引用和 78 个唯一验收项的现有语义对应，不用“页面可点击”替代后端验收。

## 8. 实施优先级

规格与原型层面已无 P0 阻断。产品实现仍应按六阶段计划设置硬性准入：

1. **Phase 1：领域模型、数据库与迁移基础**——Revision、Candidate、Artifact、引用、任务 owner、journal、备份与恢复。
2. **Phase 2：项目/剧集/导入**——18 页 Shell 中的项目、剧集、六来源、外部 AI 与五步导入。
3. **Phase 3：剧本与素材**——剧本版本、项目素材、个人库、本集素材和内部快照。
4. **Phase 4：分镜与生成**——Shot/Segment、图片、H3、Reference Manifest、任务与候选。
5. **Phase 5：成片与交付**——审核、时间线、声音、开始合片及内部 Picture/Base/Post/Delivery 链。
6. **Phase 6：正式迁移与切换**——冻结、备份、journal、事务迁移、对账、路由切换、删除旧实现、失败恢复。

自由创作和高级画布可在核心四阶段稳定后实现，但属于已确认范围，不需要运行期 V1/V2 feature flag。完整 2D、完整 3D 和视频重绘不得混入这六阶段。

## 9. 准入标准与最终判定

进入 `writing-plans` 的条件现已满足：

- 18/18 注册页面均有页面级原型，不存在“路由有名但只有评审占位”的页面；
- 52/52 能力具有唯一覆盖映射；
- 六来源、外部 AI、五步导入、剧本确认、资产快照、逐镜生成、合片、超分和交付形成可操作链；
- Cut 的 Video/Picture/Delivery Gate 与默认 UI 一致；
- 基础合片和超分决策可追溯；
- 迁移口径与最新架构母稿一致，不再把旧渐进双轨假设作为阻断；
- 自由创作/Canvas 与排除的完整 2D/3D/重绘边界明确；
- 原型状态和产品代码状态始终分开表述。

最终判定：**架构方向通过；个人创作者规格执行关口通过；收敛后的统一原型评审通过；可以重写可执行的 V2.1 六阶段产品代码实施计划；产品代码开发尚未开始。**

## 2026-09-09 任务中心与高级画布复审补充

本轮复审将两页从“有入口/有状态展示”提升为可恢复的原型契约：

- 全局任务中心（J-01）仍是 Product Core/P0。任务按下一步责任人分组，补齐项目/剧集/阶段/对象上下文、可信进度、成本台账、完整时间链、错误与重试语义、离线行为、深链接聚焦、详情 Drawer 和批任务部分成功；不扩展为团队任务管理、钱包或消费中心。
- 高级画布（X-01）仍是 OPTIONAL/P2。补齐有方向关系线、stale 表达、节点到标准页精确跳转、只读影响分析、批量重编译影响预览/确认/任务中心落点和布局保存状态；不增加第二套资产/候选/任务数据，也不复制完整 2D/3D 导演台。
- 本轮更新仅涉及统一原型模型、交互 UI 规格、页面决策记录和原型测试；真实任务调度、Provider 回调、账单、画布渲染引擎和持久化布局尚未开发。

因此，当前结论保持为：架构与产品边界通过；上述两页的原型交互已按评审意见修订；进入产品代码开发前仍需按 Product Core 和 Platform P0 分别验收。

## 2026-09-09 AI 配置复审补充

AI 配置页评审意见成立，已吸收为新的原型与规格合同。保留分层默认、项目覆盖和任务快照，同时明确安装默认与单次覆盖的解析顺序；默认 UI 不暴露不必要的密钥、hash、fingerprint 和运行时术语。默认通道卡补齐任务类型、网络/本地、费用、可信进度、取消/暂停、用户在场和可用性；ChatGPT 环境检查补齐检查时间、诊断、新任务阻断与运行中影响。

本轮同步补齐 Provider 编辑与零费用能力探测、按能力分组的业务映射、脱敏导入导出逐项冲突、一次性通道回到任务配置 Drawer、以及 Provider/模型/凭据/代理/证书/本地服务/快照不一致等失败恢复场景。失败和冲突进入 Drawer 或诊断目标，不再以 Toast 作为唯一反馈。

本轮仅修改统一原型模型、统一 HTML、交互规格、页面决策记录和原型测试；未修改 `src/`、数据库、真实 Provider、ChatGPT 浏览器桥接或 `CHANGELOG.md`。原型完成不等于 AI 配置产品实现完成，实施时仍需按 Platform P0（安全存储、能力探测、快照一致性、任务恢复）与 Product Core 分开验收。

## 2026-09-09 常规设置复审补充

常规设置评审意见成立，路径安全和保存恢复被提升为 Platform P0。页面继续只承载低频基础设施配置，不重复建设高级数据工具；但工作区、媒体、成片和临时目录不再作为普通文本表单直接保存。

本轮补齐了 clean/dirty/saving/saved/failed/conflict 保存状态、路径存在性/权限/空间/重叠/活动任务检查、目录离线影响范围、备份保留限制与立即备份、默认值作用域说明，以及工作区迁移向导（迁移范围预览、数据库/媒体复制、原目录保留、活动任务阻塞、回滚点和重新打开提示）。复杂完整性检查、媒体重定位、迁移 journal、备份恢复和物理清理仍进入高级数据工具。

本轮仅修改统一原型模型、统一 HTML、交互规格、页面决策记录和原型测试；真实文件选择器、目录校验、迁移执行、备份服务和回滚能力尚未开发。产品实施需将路径安全、任务冻结和失败恢复作为 Platform P0 单独验收。
