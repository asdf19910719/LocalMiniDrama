# LocalMiniDrama Production Studio V2.1 集成验收报告

> 日期：2026-09-11
> 分支：`codex/v2-1-implementation`（自 `codex/v2-1-personal-prototype` 拉出）
> 依据：《V2.1 产品代码实施计划》（`docs/superpowers/plans/2026-09-11-production-studio-v2.1-implementation.md`）、架构母稿与五份 P0 附件、2026-09-10 三份收敛设计、统一原型（行为合同）
> 测试命令与结果：后端 `node --test test/*.test.js`（Node 22.22.3）→ **1026 pass / 0 fail**；前端 `node --test test/*.test.js` → **194 pass / 0 fail**；原型回归 → **174 pass / 0 fail**；`frontweb && npm run build` 成功；`git diff --check` 干净

## 1. 验收结论

**通过（附遗留项）**。两条核心剧本在 API 集成测试与 GUI 黑盒走查双通道全绿；六阶段门禁全部完成；正式数据迁移已在真实库执行（journal `COMMITTED`，备份 `data/backups/v2.1/v21_*`）；旧制作页面（FilmList/DramaDetail/FilmCreate/DramaCanvas）已删除，canonical 路由切换为 `/projects...`。遗留项见 §6，均为 P1/P2 范围补充，不阻断两条核心剧本。

## 2. 剧本一：业务主流程（逐步判定）

| # | 操作 | 预期 | 实际 | 判定 |
|---|---|---|---|---|
| 1 | 项目列表 → 新建项目（名称/画幅/题材） | 表单无时长字段；名称空时创建禁用；创建后进概览 | 表单仅名称/画幅/题材/简介；空名禁用；创建后跳 `/projects/8` | 通过 |
| 2 | 概览编辑资料、更换画面风格（中央弹窗三页签） | 预设/我的/自定义三页签；应用提示"只影响之后的新生成" | 弹窗三页签正确；169 风格目录渲染；rh-101→rh-242 应用成功且 Toast 文案正确 | 通过 |
| 3 | 剧集页"新建剧集" | 只需集号（可选标题），直达空白剧本 | 弹窗仅集号；确认后直达 `/episodes/9/script` | 通过 |
| 4 | 粘贴剧本产生草稿，场次结构可见可编辑 | 保存即解析场次；自动保存状态可见 | "已保存于 HH:mm:ss"；场次结构列出 2 场；409 冲突路径在集成测试断言 | 通过 |
| 5 | "确认剧本" | 批准版本；CTA 转为进入设定 | 确认成功，出现"进入本集设定" | 通过 |
| 6 | 本集设定：只显示本集引用对象；为缺失形象生成图片 | 三 Tab 引用卡；缺形象徽标；生成入候选、点击设为当前 | 林夏卡显示"缺少可用形象"；生成产出真实 PNG 候选；点击候选→当前图→readiness 转"已准备好" | 通过（含 D1 缺陷，见 §5） |
| 7 | 点击"进入分镜" | 立即跳转；状态条显示后台准备；结构可编辑 | 立即跳转；状态条"素材已准备"；时段编辑器可编辑 | 通过 |
| 8 | 生成分镜图与镜头视频（mock），逐镜采用 | 图片候选→设为当前→H3 需更新→视频候选→载入播放器→"用于本镜" | 两镜全流程走通；候选"未自动采用"；采用后"用于本镜"标记 | 通过（含 D2 缺陷，已修复） |
| 9 | 未就绪素材场景 | 受影响镜头生成禁用 + 唯一"去处理"恢复 | 补图前 readiness"有 1 项可稍后处理"；守卫 API 禁用并返回 studio-assets 恢复入口（集成测试断言） | 通过 |
| 10 | 成片：审片、整集合成、导出 MP4 | 硬门禁 x/y；合成 vN；导出真实文件 | 2/2 后合成产出 `episode-9-v1.mp4`（真实 ffmpeg 拼接）；导出 MP4 落盘并记录 SHA-256；SRT 时间轴正确 | 通过 |

## 3. 剧本二：外部 AI 制作包回流（逐步判定）

| # | 操作 | 预期 | 实际 | 判定 |
|---|---|---|---|---|
| 1 | 剧集页"导入 / 协作 → 外部 AI 制作" | 独立 Page 向导 | 8 步向导页打开 | 通过 |
| 2 | 选择目标 | 创建下一集或填充空白集；非空剧集永不可写入 | 单选 + 保护文案；空白集下拉仅列空白集 | 通过 |
| 3 | 自动汇总上下文（只读）+ 唯一可编辑补充说明 | 上下文只读；仅 task-note 可编辑 | 步骤 2 只读；步骤 3 仅补充说明输入 | 通过 |
| 4 | 创建任务包 | package_id/assets_digest 冻结；下载 ZIP/单文件 JSON/复制可用；零媒体任务零费用 | `extai_*` 生成；ZIP 下载 200 有字节；页面声明零费用 | 通过 |
| 5 | 选择结果 JSON（合法夹具，external-ai-result@2.1） | 逐项校验 | 七项检查全 √（GUI 渲染逐项 √） | 通过 |
| 6 | 篡改字段验证 | package_id 错误被拒 | "校验未通过"，package_id 检查 ×；digest 篡改在导入适配层拒绝（ASSETS_DIGEST_MISMATCH，集成测试断言） | 通过 |
| 7 | 五步导入预览 | 新建/复用/场次/分镜/时段汇总；媒体任务 0 | 预览显示目标、场次 1 · 分镜 1 · 时段 1、新建 2 · 复用 1 · 媒体任务 0 · 远端费用 0 | 通过 |
| 8 | 导入后确认 | 目标剧集草稿、剧本未自动确认、零图片/视频/音频任务、来源审计四类只读证据 | episode_imports 绑定 package_id/schema 2.1/哈希/报告；剧本状态 draft；async_tasks 计数为 0；重复导入 409；剧集行显示"外部来源"只读入口 | 通过 |
| 9 | 打开剧本页人工检查 | 草稿可编辑、未确认 | 第 2 集草稿打开，标题/场次结构正确，CTA 为"确认剧本"（未确认态） | 通过 |

## 4. 集成测试（验收 A）覆盖

`backend-node/test/v21Integration.e2e.test.js`（真实 express 应用 + 临时库 + 全量 /api/v2 路由，HTTP 断言）：

- 剧本一全链（同上表 API 通道）：项目元数据无时长断言、概览聚合、REVISION_CONFLICT 409、mock 图片真实落盘、快照指纹、联合检查四行、数量越界 400、合成真实 MP4、导出 hash/SRT；
- 剧本二全链（同上表 API 通道）+ 幂等（重复导入 409）；
- 异常路径：注入 failed 快照 → readiness=`snapshot-failed` → 守卫禁用且唯一恢复入口指向本集设定；任务完成重复调用不产生重复候选；任务取消保留记录、重试创建新 attempt（mock 单测 + 集成联测）；
- 被复用/改造模块与新链路贯通验证：五步导入引擎（episodePackageService 的 source_key 稳定键 + `ensureStableAssetKeys` 被向导复用）、H3 草稿表、director 候选三表（唯一候选事实源）、async_tasks 任务契约、ffmpeg 合并链。

## 5. 发现并修复的缺陷 / 遗留缺陷

| 编号 | 发现 | 处置 | 状态 |
|---|---|---|---|
| D1 | GUI 校验发现：`buildDownload` 调用 `getTaskBundle` 签名错误导致任务包 ZIP 下载 500 | 修正调用并补断言（下载 200 且有字节） | 已修复 |
| D2 | GUI 走查发现：分镜页"进入成片审核（x/y）"在采用后未即时刷新（数据层 2/2 正确，刷新后显示正确） | `adopt/undoAdopt` 后同步刷新 completion | 已修复 |
| D3 | GUI 走查发现：外部 AI 适配对"没有任何人物状态"的既有人物校验失败（states minItems 1） | 适配器为无状态既有角色合成默认状态（默认标记 true） | 已修复 |
| D4 | 集成夹具错误（结果未声明所引用场景资产）被校验器正确拒绝 | 属夹具错误非产品缺陷；校验行为符合"引用未解析必须拒绝" | 非缺陷 |
| G1 | 本集设定页缺"从剧本解析/从项目素材添加引用"的显式 GUI 入口（引用派生自剧本关联与 selections；本次走查经 API 建立 selection） | 后端投影/快照/守卫正确；GUI 入口为 P1 补充 | 遗留（P1） |
| G2 | 剧集页"小说/长文本拆集""从已有视频开始剪辑"仅有菜单入口（后端 API 已就绪：import-novel 预览/确认、source-video 登记） | 向导页 GUI 化为 P1 | 遗留（P1） |
| G3 | 任务中心为聚合只读视图（项目/剧集/类型/通道筛选、深链聚焦为 P1）；设置页为入口聚合 | 遗留（P1/P2） | 遗留 |

## 6. 复用 / 改造 / 重写最终台账

| 对象 | 处置 | 评估结论 | 改造内容 | 遗留风险 |
|---|---|---|---|---|
| dramas/episodes/storyboards/characters/scenes/props 表体系 | 直接复用 | 字段语义与 V2.1 一致，避免第二套事实源 | 仅加列（storyboards: structure_revision/image_prompt_manual/script_revision_id；async_tasks: input/cost/owner/idempotency/cancel；external_ai_package_tasks: task_note/context_version/cancelled_at；episode_cut_versions: updated_at） | 低；旧列（video_url、script_content）仅作一次性迁移输入 |
| migrations 启动迁移 + ensureColumns 机制 | 直接复用 | 满足"启动自动迁移"，36 号增量幂等 | 新增 36_v21_domain.sql + v21/db.js 专属 ensure | 低 |
| episodePackageService（旧 1.x/2 协议引擎） | 改造复用 | 其 2.1 校验/投影不可用（旧协议），但 source_key 机制、写入模式复用 | V2.1 新建导入引擎（packageContractV21/episodeImportV21）；旧引擎服务旧路由至 Phase 6 删除 | 低：V2.1 入口无旧协议分支 |
| externalAiTaskBundleService/externalAiResultContract | 直接复用 | 任务包协议（package_id/digest/manifest/schema）与 V2.1 一致 | 加 task_note/context_version/cancelled_at 三列；适配层按 V2.1 schema 重建 | 低 |
| 风格系统（styleRegistryService 169 风格） | 直接复用 | "画面风格"语义一致，应用只影响新生成 | V2.1 项目创建应用安装默认风格；概览中央弹窗消费目录 | 低 |
| H3 链路（h3PromptDraftService/编译器/校验器） | 改造复用 | 真实 H3 编译依赖 Provider/工作流配置；V2.1 无 Key 场景需确定性通道 | V2.1 `h3DraftFacade`（storyboardService 内）：复用 storyboard_h3_prompt_drafts 表与四项校验/状态机语义，编译为确定性文本；真实 H3 Provider 链保留待接入 | 中：mock H3 与真实 H3 编译产物形态需在接入真实 Provider 时对齐 |
| director_candidate_groups/candidates/artifacts | 改造复用 | 唯一候选事实源（迁移矩阵 V-02） | V2.1 以 async_tasks id 作 job_id 写入三表；adopted=selected_candidate_id | 低 |
| videoMergeService/video_merges | 改造复用 | 整集合片+混音能力保留 | V2.1 `cutService` 以 ffmpeg concat 合成 + episode_cut_versions 版本化包装；混音深链待接 | 中：成片设置（BGM/TTS/超分）当前只记录设置，混音执行在真实媒体接入时启用 |
| async_tasks/taskService | 直接复用 | 进度/取消/重试契约满足任务中心 | 加列与幂等键唯一索引 | 低 |
| imageGenerationOrchestrator 多通道 | 改造复用 | V2.1 首发以 mock 通道满足无 Key 可运行；多通道接入为配置问题 | V2.1 候选/当前图模型建立在其上（image_generations + image_generation_tasks） | 低：ChatGPT 网页/ComfyUI 通道接回时复用同一候选/当前图语义 |
| projectDeletionService | 改造复用 | 项目删除生命周期已实现 | V2.1 项目/剧集回收站复用软删除模式 + 影响清单 | 低 |
| novelImportService | 直接复用 | 分章/拆集逻辑满足 | V2.1 路由薄封装 | 低 |
| FilmCreate/DramaDetail/FilmList/DramaCanvas 及其测试 | 重新实现并删除 | 单页长流程/旧信息架构与 V2.1 契约冲突（A-MG03） | 新 productionStudio 页面族 + v21 api/view 层；旧视图与 19 个旧源合同测试删除 | 低：被删能力的 V2.1 等价物均有测试 |

## 7. 与 78 项 E2E 验收矩阵覆盖对照

**完整覆盖（两条剧本 + 集成/单元测试双重证据）**：A-P01（项目列表/新建/概览，GUI+API）、A-E01（剧集中心，GUI+API）、A-E03（集序冲突 409）、A-I01（五步导入全量错误码/TARGET_NOT_BLANK/零媒体任务/幂等）、A-I02（外部 AI 全闭环含篡改拒绝）、A-I03（来源只读审计）、A-ST01～03/05（三层状态并存/提交批准/stale/409）、A-S01/S04/S05（自动保存与冲突/确认/一次性剧本迁移）、A-A01/A02（候选使用与进入分镜/跨集精确版本快照）、A-A06（引用保护删除）、A-B01/B02（结构创建与时段编辑/409）、A-H02/H03（H3 一等公民校验与 stale/人工不覆盖）、A-V01/V02/V04（视频提交/采用/守卫/完成度/历史重试）、A-G02 部分（任务契约）、A-T02～T06（门禁/合成/失败保留/导出/版本）、A-MG01～MG04（迁移对账/失败恢复/旧实现删除/journal）。

**部分覆盖（后端+服务层证据，GUI 入口 P1/P2）**：A-P03/A-P04（小说/ZIP 后端在旧路由已有，V2.1 入口 G2）、A-A03～A05/A07（人物状态/资产库/外部匹配——后端断言 + 向导映射校验）、A-B03～B06（预检/时长呈现/图片提示词/首尾帧——服务层+部分 GUI）、A-G01/G03～G05（多通道——mock 通道全量，真实通道接回即用）、A-AU01～AU05（声音策略记录+混音内部执行）、A-J01/J02（任务中心最小视图）。

**未进入默认界面（P2，与评审报告"自由创作和高级画布可在核心四阶段稳定后实现"一致）**：A-X01/X02（高级画布）、A-F01（自由创作改版）、A-M01（媒体库 V2.1 改版）、A-L01～L03 的 Look Probe/场次覆盖层（场景 Look 覆盖已从 09-10 默认界面删除）、A-ST04 豁免的 GUI（豁免 API+门禁集成已验证）、A-C02/C03 高级配置运维（保留旧页可用）。

以上未覆盖项均不属于两条核心剧本；其协议与数据边界已在领域层预留（mock Provider Adapter、豁免表、候选事实源）。

## 8. 正式迁移执行记录

- 副本演练：`data/backups/v2.1` 机制在真实库副本上演练通过（7 集→7 剧本版本、60 分镜→60 时段、对账 ok、COMMITTED）；
- 正式执行：冻结后端 → `runV21Migration` 于真实库 → journal `v21_*` 状态 `COMMITTED`（sourceVersion/targetVersion 2.1.0、备份与 hash 记录完整）→ 后端重启后 `/health`、`/api/v2/projects` 正常；
- 旧实现删除：四类旧制作视图与旧路由已移除；旧协议导入引擎随旧路由停用（无 V2.1 入口可达，符合"不做运行期旧协议升级"）。

## 9. 遗留风险与建议

1. **G1/G2（P1）**：本集设定引用入口与小说/已有视频向导 GUI 化——建议下一迭代优先补齐，避免个人用户需要理解"selection"概念。
2. **真实 Provider 接回**：mock 通道契约与真实 Provider 对齐（H3 编译产物、多通道生图、混音执行），接入时沿用同一候选/采用/快照语义，无需改表。
3. **性能目标**：镜头轨虚拟列表、任务轮询聚合（母稿 §20.3）尚未实现；当前镜头量（<100）无感知，200 镜项目前建议压测。
4. **Node 版本**：本机 PATH 为 Node 24，项目固定使用 Node 22.22.3（与 better-sqlite3 ABI 对齐）；建议在 package.json engines 提示处增加启动时 ABI 校验提示。

## 10. R2 轮次（35 张设计稿全面对齐）· 2026-09-11 增补

依据 `docs/design/v2.1-ui/`（35 张：23 页面 + 12 容器）完成前端全面改造：

| 范围 | 结果 |
|---|---|
| 设计系统 | `shared/ui.css` 全量令牌与组件以 `.v21-root` 作用域接入（不影响旧页浅色主题）；Rail + 43 项 SVG 图标 sprite |
| 页面（23） | 全部按稿重建：01 项目列表大封面卡、02 新建来源卡、03 概览 Hero+待处理+四阶段汇总、04 剧集阶段格、05 外部 AI 向导、06 制作包导入、07 素材统计分组、08 剧本三栏、09 本集设定+详情抽屉、10 分镜五区、11 成片单屏、12 任务中心、13 资产库、14 常规设置、15 AI 配置、19 归档导入、20 数据工具、21 自由创作、22 高级画布、23 媒体库、24 工作区向导、35 物理清理 |
| 容器（12） | 16 生成 Sheet、17 风格 Modal、18 影响摘要、25 批量预检、26 引用管理、27 素材预览、28 生成历史、29 版本比较、30 剧本历史、31 编辑项目、32 删除确认、33 资产库详情、34 音色 |
| 交互合同补齐 | Ctrl+S 保存、[ ] 切镜、选段 AI 浮层（改写/扩写/缩写）、确认影响摘要与下游影响、预计素材变化、批量预检与三类批量动作、生成历史按原输入重试、删除影响预检+回收站 30 天文案 |
| 后端补齐 | 场次统计/确认预检/版本 diff/阶段导航 meta（剧本）、批量预检+三类批量+生成历史+任务重试（分镜）、删除影响预检（剧集）、自由创作 mock 端点 |

验证：后端 1030 pass / 0 fail；前端 194 pass / 0 fail；原型 174 pass / 0 fail；构建成功；GUI 批量走查十页全部命中关键标记；截图确认项目列表深色全屏与设计稿 01 一致。

遗留（全部为执行器级而非 UI 级，UI 已按设计稿呈现）：工作区迁移真实执行器、媒体重定位扫描器、物理清理真实删除执行器（UI 为设计态+安全文案）；高级画布节点拖拽编排（只读投影即 22 号稿口径）。
