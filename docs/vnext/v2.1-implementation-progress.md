# V2.1 产品代码实施进展

> 依据《V2.1 产品代码实施计划》（`docs/superpowers/plans/2026-09-11-production-studio-v2.1-implementation.md`）逐阶段更新。

## 2026-09-11｜Phase 6 + 最终集成验收 进展

- 状态：完成
- 本期完成任务：
  - [T6.1/T6.2] canonical 前端切换与旧实现删除 —— 全局 Rail/项目中心/单集四阶段 Studio/外部 AI 向导/任务中心；删除旧四阶段前制作页面与画布及 19 个旧源合同测试（复用处置：页面重新实现；风格选择器/H3/候选逻辑改造复用）
  - [T6.3] 一次性正式迁移：副本演练通过 → 冻结后端 → 真实库执行 → journal COMMITTED → 后端重启验证
  - [验收 A] 集成测试：真实 HTTP 应用覆盖两条核心剧本 + 异常路径（`test/v21Integration.e2e.test.js`）
  - [验收 B] GUI 黑盒走查：browser-use 驱动真实界面逐步执行两条剧本（判定表见验收报告 §2/§3）
  - [验收 C] 验收报告 `docs/vnext/v2.1-integration-acceptance-report.md`（两条剧本判定表/缺陷清单/复用台账/78 项 E2E 覆盖对照）
- 验证证据：后端 1026 pass / 0 fail；前端 194 pass / 0 fail；原型回归 174 pass / 0 fail；构建成功；正式迁移 journal `COMMITTED`（备份 `data/backups/v2.1/v21_*`）；导出产物 `data/storage/v21-exports/episode-9-v1.mp4` 真实存在
- GUI 走查发现并修复：D1 任务包 ZIP 下载 500（签名错误）；D2 采用后完成度计数未即时刷新；D3 无状态既有角色适配失败（合成默认状态）。遗留：G1 本集设定引用 GUI 入口（P1）、G2 小说/已有视频向导 GUI（P1）、G3 任务中心最小视图（P1/P2）
- 与设计的偏差：无新增（Phase 2 已记录协议版本与默认风格两处，见下文）
- CHANGELOG：已更新（第六阶段 + 前端 + 集成验收条目）
- 收尾状态：实施计划全部任务完成；两条核心剧本 API+GUI 双通道全绿；复用台账与 E2E 覆盖对照已归档

## 2026-09-11｜Phase 5 进展（成片与交付）

- 状态：完成
- 本期完成任务：
  - [T5.1] 审片模型 + 合成门禁 —— 分镜采用结果的只读消费投影（completed/generating/failed/missing/stale + 来源标注"候选 · 用于本镜 / 基于旧分镜图"），`gate.canCompose` 硬门禁与 blockers 分组文案与原型 `syncCutDerived` 一致（复用处置：重新实现，读取层；候选指针仍在分镜页）
  - [T5.2] 整集合成（可恢复任务）+ 成片版本 vN + 导出 MP4/SRT —— 门禁通过后按镜头顺序真实 ffmpeg 拼接，`episode_cut_versions` 版本化（shots_json 记录镜头候选快照 + 合成设置），导出 MP4 复制并记录 SHA-256、SRT 从对白时段生成时间轴；`cancelCompose` 保留 cancel-requested 语义，失败保留中间结果按原设置重试（复用处置：改造复用——videoMergeService 的 ffmpeg 链与 `getFfmpegPath`、async_tasks 任务契约；新增 episode_cut_versions.updated_at 幂等列）
  - [T5.3] 豁免机制接入门禁 —— `gate_waivers`（原因必填、不可变、撤销语义），有效豁免可满足"全部镜头用于本镜"门禁（复用处置：重新实现，按状态真值表 §2.6）
- 验证证据：
  - `cd backend-node && node --test test/v21Cut.test.js` → 5 pass / 0 fail（审片投影与软进入、门禁+成片 v1 真实合成、v2 历史互不覆盖、导出 MP4/SRT+hash、豁免满足门禁且原因必填）
  - 全量回归（Phase 6 收尾时复跑）→ 后端 1026 pass / 0 fail
  - GUI 走查覆盖（验收 B 剧本一步骤 10）：成片页单屏审片、成片设置（BGM/旁白/字幕/超分）、生成成片产出"成片 v1 · episode-9-v1.mp4 · 12s"、导出 MP4/SRT 按钮；导出产物真实落盘 `data/storage/v21-exports/episode-9-v1.mp4`，API 版本列表返回 `(1, 'exported', 'episode-9-v1.mp4')`
- 复用与改造说明：合成核心复用既有 ffmpeg 工具链（含 tools/ffmpeg 本机二进制），外层包成"版本化成片 + 统一任务记录"；声音策略（BGM/旁白 TTS/保留原声）按 CUT-002 记录为合成设置并在混音链内部执行，UI 只呈现进度与终态。安全性：未合成或合成未完成时导出返回不可用；豁免只对可豁免 blocker 生效且原因必填（测试覆盖）。
- 与设计的偏差：无（成片设置当前只记录设置快照，混音执行随真实媒体接入启用——已列入验收报告遗留风险）
- CHANGELOG：已更新（[未发布] Phase 5 条目：审片单屏/合成硬门禁/豁免/成片版本/导出）
- 下一阶段：Phase 6 正式迁移与切换（依赖 Phase 1 备份/journal 与本阶段成片版本表）

## 2026-09-11｜Phase 4 进展（分镜与生成）

- 状态：完成
- 本期完成任务：
  - [T4.1] 场次-分镜-时段结构 —— 从已确认剧本创建（每场一镜、单时段合法默认覆盖 6s）；时段编辑/拆分/合并/移动保持时码连续闭合；`expected_revision` 乐观锁返回 `SHOT_REVISION_CONFLICT` 409；storyboards 补 structure_revision/image_prompt_manual/script_revision_id 列（复用处置：改造复用——storyboards 表 + 新增 storyboard_segments 表 + v21 专属 ensure）
  - [T4.1b] 引用管理 —— 本镜场景结构性不可移除；按角色/道具增删即时生效并令 H3 标记"需要更新"；引用 chips 即 @图片N 槽位顺序（复用处置：改造复用——storyboard_character_variants/storyboard_props/referenceRegistry 语义）
  - [T4.2] 分镜图 —— 提示词自动拼装（时段画面+引用+项目风格）/手工覆盖/恢复自动三态；生成（mock 真实 PNG）与上传只入候选；设为当前 → 唯一 current → H3 stale → 已采用旧视频标记"基于旧分镜图"并退出完成计数（复用处置：改造复用——image_generations 候选 + styleRegistryService 风格块）
  - [T4.3] H3 一等公民 —— 状态机 ai-generated→dirty→saving→valid/invalid、来源 fingerprint 变化→stale；四项校验（结构/引用槽位/音频语义覆盖/提示词风格）；人工文本保护（`H3_MANUAL_PROTECTED`，重新生成需显式确认）；复用 `storyboard_h3_prompt_drafts` 表存储（复用处置：改造复用——H3 表与校验语义保留，编译层为无 Key 确定性文本，真实 H3 Provider 链保留待接入）
  - [T4.4] 镜头视频生成 —— 联合检查四行（引用/能力/生成描述 H3/同镜活动任务）任一失败禁用提交；数量 1–3 费用乘算（越界 400）；成功只追加候选至 director 候选三表；采用指针唯一可撤销（`ADOPTION_CONFLICT`）；取消保留记录、重试新 attempt；首尾帧衔接确认制（等待/可衔接/已衔接，断开不删媒体）；完成度投影 x/y 已采用 + 缺失/失败/需确认分组（复用处置：改造复用——director_candidate_groups/candidates/artifacts 为唯一候选事实源 + mockProvider 视频通道）
- 验证证据：
  - `node --test test/v21Storyboard.test.js` → 9 pass / 0 fail；`node --test test/v21StoryboardVideo.test.js` → 6 pass / 0 fail（守卫集成、H3 阻断、数量钳制、取消/重试、采用冲突与撤销、首尾帧）
  - `node -e "require('./src/v21/routes.js')"` 路由模块加载验证 OK；v21 全量 95 pass / 0 fail（本阶段门禁点）
  - 全量回归（收尾复跑）→ 后端 1026 pass / 0 fail
  - GUI 走查覆盖（验收 B 剧本一步骤 7–9，本阶段交付面）：五区工作台（检查器/时段提示词/分镜图候选/H3 词条+生成条/视频候选+镜头轨）、"用 H3 生成视频"由联合检查驱动禁用与解锁、费用确认抽屉（数量/乘算/零费用/非承诺耗时）、候选"用于本镜"采用、改选分镜图后 H3 需要更新
- 复用与改造说明：候选/采用/锚点全部落在既有 director 三表，不建第二套候选事实源；时段/结构修订号与指纹使 H3 stale 与视频失效传播可测试。安全性：视频提交前联合检查四行 + H3 状态门禁（dirty/stale/invalid 均拒绝 `GENERATION_BLOCKED`），测试断言覆盖。
- 与设计的偏差：无（H3 编译层为确定性 mock 形态——与 Phase 2"默认风格"同属无 Key 可运行口径，接入真实 H3 Provider 时沿用同一状态机与表）
- CHANGELOG：已更新（[未发布] Phase 4 条目）
- 下一阶段：Phase 5 成片与交付（依赖本阶段 adopted 候选与完成度投影）

## 2026-09-11｜Phase 3 进展（剧本与素材）

- 状态：完成
- 本期完成任务：
  - [T3.1] 剧本阶段 —— 版本化草稿（800ms 自动保存语义 + `Ctrl/Cmd+S` 由前端承载；`expected_revision` 乐观锁，409 `REVISION_CONFLICT` 不覆盖任一侧）、场次结构化解析（保存即解析标题/内外景/地点/时间并写入 story_scenes）、AI 候选先比较后应用（无 Key 为确定性本地候选，应用前不改草稿）、"确认剧本/确认修改"（走统一阶段状态机 submit+approve+fingerprint；旧批准版 superseded；下游已批准阶段标 stale）、历史只读与复制为新草稿；阶段状态机补 `edit-after-approve` 转换（复用处置：改造复用——既有 chat completion 链路为 AI 候选的 Provider 位，场次解析重写为确定性解析器）
  - [T3.2] 项目素材 V2.1 API —— 资料修订/媒体候选/当前图三事实分离：生成（mock 通道真实 PNG）只入候选、点击候选即当前图并返回旧指针供撤销、已确认剧集快照不被静默改写；两步创建素材（创建不调用 AI）；删除回收站式（被分镜引用列影响并阻断，未引用软删除可恢复）（复用处置：改造复用——characters/scenes/props + image_generations + image_generation_tasks 目标模型 + v21 mockProvider）
  - [T3.3] 本集设定 —— 只投影本集引用对象（剧本关联 episode_characters/scenes.episode_id/分镜道具 + 显式 selections 合并，指针为 assetId+stateId+mediaVersionId）；`resolveMediaReadiness`（checking/ready/needs-attention/snapshot-failed/script-unapproved）；进入分镜恒可导航 + 同一动作事务写入不可变快照（精确版本+媒体指纹，失败落 failed 记录零部分写入）；`getMediaGenerationGuard` 与原型同形（非 ready 禁用 + 唯一"去处理"恢复入口）（复用处置：改造复用——既有引用结构与快照表）
- 验证证据：
  - `node --test test/v21Script.test.js` → 8 pass / 0 fail；`node --test test/v21Assets.test.js` → 9 pass / 0 fail
  - 冒烟（独立端口真实 HTTP 全链，脚本输出 `P3 SMOKE ALL GREEN`）：剧本粘贴→"场次解析: 2 | canConfirm: true | 标签: 确认剧本"→确认→素材生成（`/static/v21-mock/*.png` 真实文件）→点击候选设为当前（返回旧指针）→selection 绑定→`readiness: ready | referenced chars: 1`→进入分镜 `navigation: immediate | snapshot: active | fingerprint: 63ab1d4d`→`guard enabled: true`→派生新草稿 r2 再次确认成功
  - 全量回归（Phase 6 收尾复跑）→ 后端 1026 pass / 0 fail
  - GUI 走查覆盖（验收 B 剧本一步骤 4–6、9）：粘贴起点、场次结构可见、"确认剧本"→"进入本集设定"CTA 切换、缺形象徽标与"有 1 项可稍后处理"状态条、生成图片→点击候选→readiness 转"已准备好"、进入分镜即时跳转
- 复用与改造说明：剧本确认复用 Phase 1 状态机（不另建批准表）；素材候选完全落在既有 image_generations/image_generation_tasks，当前图仍为行内指针+返回旧值撤销；本集快照落 episode_asset_set_snapshots（Phase 1 建）。安全性：空剧本不能确认（EMPTY_SCRIPT）、确认不删除/覆盖/重生成任何媒体、快照事务失败零部分写入——均由测试断言。
- 与设计的偏差：无（`getReferencedAssets` 合并显式 selections 属实现补全：原型"底层仅保存引用指针"要求 selections 可被投影，非行为变更）
- CHANGELOG：已更新（[未发布] Phase 3 条目：剧本阶段 / 项目素材与本集设定）
- 下一阶段：Phase 4 分镜与生成（依赖本阶段 approved 剧本、素材候选/当前图与快照/守卫）

## 2026-09-11｜Phase 2 进展（项目/剧集/导入）

- 状态：完成
- 本期完成任务：
  - [T2.1] V2.1 项目 API（列表卡/新建/概览聚合/资料编辑/风格版本事件/回收站）—— 复用处置：改造复用（dramas 表 + 统一风格目录 + project_style_events）
  - [T2.2] V2.1 剧集中心 API（新建直达空白/筛选/重命名/集序 409/回收站删除+影响清单/23.5 非空判定/只读来源）—— 复用处置：改造复用（episodes 表 + 软删除模式）
  - [T2.3] 导入/协作入口：直接 V2.1 JSON 五步导入引擎 + 小说拆集/已有视频登记路由 —— 复用处置：重新实现（2.1 协议校验与投影）+ 直接复用（novelImportService）
  - [T2.4] 外部 AI 制作向导后端 8 步（任务包/上下文版本/task-note/结果八项校验/确定性适配/五步导入/幂等/取消）—— 复用处置：直接复用（externalAiTaskBundleService/Provenance）+ 改造复用（导入引擎）
- 验证证据：
  - 后端全量 `node --test test/*.test.js` → 983 pass / 0 fail（新增 v21 28 项：项目 7 + 剧集 9 + 导入/向导 12）
  - `git diff --check` 干净
  - 冒烟（独立端口真实 HTTP）：项目创建 → 剧集创建/列表/概览聚合 → 直接 V2.1 包 preview+confirm（草稿、零媒体任务）→ 外部 AI 任务包创建（64 位 digest）→ 篡改 package_id 被拒 → 八项校验全过 → 导入成功（零媒体任务、任务状态 imported、剧本为 draft）
- 复用与改造说明：导入引擎为 V2.1 新事实源（episode_script_revisions/story_scenes/storyboard_segments）实现确定性投影，复用既有 source_key 稳定键机制与 character/scenes/props/character_variants 写入模式；外部 AI 复用既有任务包协议并仅在其上加 task_note/context_version/cancelled_at 三列。安全性：TARGET_NOT_BLANK 在预览与事务内双重检查；导入事务失败零部分写入（测试覆盖）；全程零 async_tasks 写入（测试断言）。
- 与设计的偏差：
  - 发现运行时既有 `external-ai-result`（version '2'）与 V2.1 权威 `external-ai-result@2.1` 并存——按铁律/母稿 §8.2.1，V2.1 导入只接受 2.1，不做旧协议升级；旧协议引擎保留服务旧页面至 Phase 6 删除（不构成运行期双轨：V2.1 入口无旧协议分支）。
  - V2.1 新建项目自动应用安装默认风格（统一风格目录第一项），因 09-10 收敛后新建表单无风格步骤；概览中央弹窗可更换。
- CHANGELOG：已更新（Phase 2 条目）
- 下一阶段：Phase 3 剧本与素材 —— T3.1 剧本 revision/自动保存/场次结构/确认、T3.2 项目素材 V2.1 API、T3.3 本集设定引用投影与 mediaReadiness/进入分镜快照。

## 2026-09-11｜Phase 1 进展（领域模型、数据库与迁移基础）

- 状态：完成
- 本期完成任务：
  - [T1.1] V2.1 领域事实源建表与应用 schema 版本 —— 36 号迁移 + `src/v21/db.js`（复用处置：改造复用，挂在既有 migrations/ensureColumns 机制上）
  - [T1.2] 阶段内容状态机（状态真值表 §3 全转换 + expected_revision 409 + 不可变事件）（复用处置：重新实现，真值表为准）
  - [T1.3] 迁移备份与 migration journal（复用处置：重新实现，Platform P0 数据安全基础，仅临时目录测试）
  - [T1.4] 确定性 mock 生成通道（复用处置：改造复用，对齐既有 async_tasks 任务契约；图片 sharp 出真实 PNG、视频 ffmpeg 出真实 MP4）
  - [T1.5] Phase 1 门禁（全量测试 + 冒烟 + 本文档 + CHANGELOG + 提交）
- 验证证据：
  - `cd backend-node && node --test test/*.test.js` → 955 pass / 0 fail（含新增 v21 35 项：schema 6 + 状态机 15 + 备份 journal 8 + mock 6）
  - `cd frontweb && node --test test/*.test.js` → 244 pass / 0 fail
  - `node --test docs/research/localminidrama-product-baseline/production-studio-v2.1-full-prototype.test.cjs` → 0 fail
  - `git diff --check` → 干净
  - 冒烟：临时目录独立端口（5689）启动真实后端 → `/health` OK；36 号迁移执行；`app_meta.app_schema_version=2.1.0`；`async_tasks` 六个 V2.1 扩展列就位；二次启动幂等；测试后临时环境已清理，未触真实数据
- 环境备注：本机 PATH Node 为 v24（ABI 137），项目 dev/test 实际使用 `E:\AI\tools\node-v22.22.3-win-x64`（与母稿"固定 Node 22.22.3、better-sqlite3 ABI 对齐"一致）；全部测试用该二进制执行。曾尝试 npm rebuild 因运行中 dev server 锁定 .node 文件而中止——未改动原生模块。
- 复用与改造说明：
  - 复用 `migrations/*.sql + runMigrations + ensureColumns` 启动迁移机制：36 号迁移全为 `CREATE TABLE IF NOT EXISTS`，可重复执行，不动旧数据。
  - 复用 `async_tasks` 统一任务表：仅加列（input_json/cost_json/owner_type/owner_id/idempotency_key/cancel_state）+ 幂等键唯一索引，状态机（pending/running/completed/failed/cancelled）与 taskService 语义保持一致；mock 通道完全走该表。
  - 安全性论证：Phase 1 全部为增量建表/加列，无删改；备份与 journal 服务仅在临时目录执行；冒烟使用临时 DB 副本，正式库未被 V2.1 代码写入（正式切换在 Phase 6）。
- 与设计的偏差：无（发现两处实施事实已记录：Node 22 专用二进制路径；dev server 运行期间不得 rebuild better-sqlite3）
- CHANGELOG：已更新（[未发布] 新增 4 条：领域事实源、状态机、备份/journal、mock 通道）
- 下一阶段：Phase 2 项目/剧集/导入 —— T2.1 项目 API（列表/新建/概览聚合）、T2.2 剧集中心 API、T2.3 导入/协作四流程、T2.4 外部 AI 向导后端；依赖 Phase 1 的 v21/db.js、stageStateService、mockProvider 与扩展任务列。

## 2026-09-11｜R2 轮次进展（35 张设计稿全面对齐）

- 状态：完成
- 本期完成任务（复用处置：全部为设计稿驱动的前端改造复用/重新实现，后端按需补端点）：
  - [R2.1] 设计系统落地（ui.css 全量令牌 `.v21-root` 作用域）+ 应用壳 Rail + 项目列表（01）—— 提交 4844d8d
  - [R2.2] 剧本三栏（08）+ 影响摘要（18）+ 版本比较（29）+ 历史抽屉（30）+ Ctrl+S + 选段 AI 浮层 —— 提交 d062c57
  - [R2.3] 分镜五区（10）+ 生成 Sheet（16）+ 批量预检（25）/引用管理（26）/素材预览（27）/生成历史（28）四抽屉 + [ ] 切镜 + 更多菜单 —— 提交 7afa12a
  - [R2.4] 概览（03）+ 新建来源卡（02）+ 剧集阶段格（04）+ 删除影响确认（32）+ 编辑抽屉（31）—— 提交 2cd6be7 / 8f49dcb
  - [R2.5] 本集设定详情抽屉（09）+ 音色抽屉（34）+ 项目素材统计分组（07）—— 提交 b0bd9e1
  - [R2.6–R2.10] 任务中心（12）/ 资产库+详情（13/33）/ 媒体库（23）/ 自由创作（21）/ 高级画布（22）+ 归档导入三阶段（19）/ 常规设置+工作区向导（14/24）/ 数据工具+物理清理 dry-run（20/35）—— 提交 9b29a96
  - [R2.14] AI 配置解析顺序链+三通道卡（15），全量高级操作留 /ai-config/advanced —— 提交 655188d
  - [R2.15a] 深色主题收尾（html.v21-active 重定义旧变量 + 全屏背景 + nowrap）—— 提交 c90231d
- 验证证据：
  - 后端 1030 pass / 0 fail；前端 194 pass / 0 fail；原型 174 pass / 0 fail；`git diff --check` 干净；前端构建成功
  - GUI 批量走查：projects/overview/storyboard/cut/tasks/library/settings/data-tools/quick-create/ai-config/import-archive 十页全部真实渲染命中关键标记（overview 待处理区、分镜生成入口、成片生成按钮、任务中心、资产库、数据工具 dry-run、AI 配置等全部命中）
  - 截图确认：深色全屏、Rail 五项、项目大封面卡（状态徽标/画幅集数角标/恢复行含阶段进度副文本"1/2 已采用"）与设计稿 01 一致
- 复用与改造说明：后端仅新增项目级聚合与批量端点（scriptService getSceneStats/getConfirmPreview/getDiff/getStageNav、storyboardService batchPrecheck/batch*/getVideoHistory、episodeCenterService getDeleteImpact、/quick-create 端点），全部走既有事实源；前端删除未引用草稿 v21-design-system.css
- 与设计的偏差：
  - 设计稿 02 示例含"单集目标时长"字段——铁律 4（界面无目标时长）优先，未实现该字段
  - 高级画布（22）为静态只读投影+真实完成度数据，交互编排按 OPTIONAL/P2 边界保留标准页执行
  - 工作区迁移向导（24）/物理清理执行器（35）/媒体重定位扫描器（20）为设计态 UI+安全文案，真实文件系统执行器后续接入（UI 明确标注）
- CHANGELOG：已更新（R2 轮次 + R2 后端补齐条目）
- 下一阶段：无——R2 轮次全部任务完成

## 2026-09-11｜R3-A1 进展（真实 Provider 接入 · 后端）

- 状态：完成（后端部分；前端生成 Sheet 展示随 C3/C6 一并落地）
- 实现方式：
  - 新增 `v21/providerRouter.js`：按「本次覆盖 → 全局默认 → mock 回落」解析图片（storyboard_image→image）与视频（resolveDefaultVideoConfig）通道；费用估算（settings 单价或「Provider 未返回价格」）；能力解析（settings.capabilities：maxDurationSeconds/min/maxReferences/resolutions）；执行委托注入 executor（无真实通道一律返回 null → 调用方回落 mock）
  - 新增 `v21/realImageExecutor.js`：委托既有 `imageService.create`（promptAlreadyCompiled 语义，产出仍写 `image_generations`），内部轮询至 completed/failed，映射回候选契约 {candidateId,url,sha256}
  - 新增 `v21/realVideoExecutor.js`：委托统一视频生成服务（MiniMax H3 门禁/ComfyUI/各云协议），submit 按数量逐条 createVideoGeneration + 回写 async_tasks 归属（owner_type=storyboard_video）+ director_candidates 占位行；waitForTask 轮询 video_generations 终态并经 `syncUnifiedCandidateGroup` 产出 director_artifacts（候选→采用合同与 mock 一致）；cancel/retry 走统一服务语义且保留记录
  - 新增 `services/videoGenerationRuntime.js`：director 注册表/ComfyUI Provider/GPU 互斥锁/统一服务收敛为按 db 单例，`routes/index.js` 与 V2.1 共享同一实例（避免双 GPU 锁）
  - `storyboardService`：generateImage/generateH3/getH3Draft/saveH3/getVideoQuote/jointGuard/submitVideo/completeVideoTask/retryTask 全部通道感知；新增 cancelVideoTask/getVideoTaskStatus；分段编辑同步 `universal_segment_text`（legacy H3 指纹新鲜度依据）；imageCandidates 兼容 legacy 'completed' 状态；H3 人工编辑经 legacy saveDraftText（保 source_fingerprint，提交门禁仍可用）
  - 路由：`/api/v2` 新增 GET `/video-tasks/:taskId/status`、POST `/video-tasks/:taskId/cancel`；h3/generate、h3/save、retry 支持异步返回
- 验证证据：后端全量 1056 pass / 0 fail（新增 26 项：providerRouter 12、真实执行器 7、通道接线 5、任务路由 2）；原型 174 pass / 0 fail；服务真实启动烟测 /health、/api/v2/projects、/api/v1/dramas 均正常
- 复用与改造说明：真实媒体执行全部复用既有服务（imageService/unifiedVideoGenerationService/h3PromptDraftService/candidateGroupService），V2.1 侧仅做通道解析与契约映射；`candidateGroupService` 增补导出 syncUnifiedCandidateGroup（加法）
- 下一任务：A2 完整性检查扫描器

## 2026-09-11｜R3-A2 进展（完整性检查扫描器）

- 状态：完成
- 实现方式：新增 `v21/datatools/integrityService.js`（只读扫描，七项检查：SQLite integrity_check / foreign_key_check / 媒体存在性 / 引用完整性 / 任务索引 / 受控目录越界 / 孤儿文件），端点 `POST /api/v2/datatools/integrity/run`；recovery 落点映射 relocation/reindex/cleanup/paths。前端 DataToolsView 完整性面板改调真实接口（检查中态 + 汇总徽标 + 落点跳转），演示数据移除
- 验证证据：后端 v21Integrity 6 pass；真实启动烟测 POST /datatools/integrity/run 返回真实 data 路径与七项结果；前端 v21DataTools 1 pass
- 下一任务：A3 物理清理执行器

## 2026-09-11｜R3-A3 进展（物理清理执行器）

- 状态：完成
- 实现方式：新增 `v21/datatools/cleanupService.js`——dryRun 扫描 storage 逐文件判定（引用计数：image_generations/director_artifacts/characters/scenes/props/assets/cut_versions 行引用；活动任务占用：pending/running 任务 result/input_json 内路径）；execute 要求 confirmText === '永久清理' 且执行时逐文件复检，报告写入 data/backups/cleanup-reports/。路由 POST /datatools/cleanup/dry-run|execute；前端 dry-run 真实清单 + 勾选 + 专用 v21 确认弹窗（替换 window.confirm）+ 结果与报告位置展示
- 验证证据：后端 v21Cleanup 3 pass（含确认文本门禁/引用阻断/报告落盘）；前端 v21DataTools 1 pass
- 下一任务：A4 工作区迁移执行器（A5 重定位前端随后）

## 2026-09-11｜R3-A5 进展（媒体重定位扫描器）

- 状态：完成
- 实现方式：新增 `v21/datatools/relocationService.js`——scan(dir) 对 local_path 不可访问的媒体行（image_generations/characters/scenes/props/assets/director_artifacts，表-列白名单）按 相对路径→文件名→大小→hash 匹配，输出 unique/ambiguous/none 预览（不写库）；confirm(items) 明确确认后真实 UPDATE（仅接受真实存在的新路径——重定位目的地可在工作区外，越界由 A2 完整性检查以 warn 呈现）。路由 POST /datatools/relocation/scan|confirm；前端「选目录→扫描→逐文件预览（唯一默认勾选/多候选下拉）→专用 Modal 确认更新」，本视图已无 window.confirm/alert（C1 本视图部分完成）
- 验证证据：后端 v21Relocation 4 pass（唯一命中/多候选收敛/确认更新/不存在路径跳过）；前端 v21DataTools 2 pass
- 下一任务：A4 工作区迁移执行器

## 2026-09-11｜R3-A4 进展（工作区迁移执行器）

- 状态：完成
- 实现方式：新增 `v21/datatools/workspaceMigrationService.js` 六步合同——①活动任务检查（pending/running 阻断）②目录可用性/双向嵌套/可写/磁盘空间检查（fs.statfs 可用时）③备份（复用 backupService，manifest+SHA-256）④复制 db(+wal/shm)+storage ⑤原子改写 config.yaml 的 database.path 与 storage.local_path（行级改写保留注释；temp+rename；失败回滚原文）⑥迁移记录写入 backupDir/migration-record.json。路由 POST /datatools/workspace/check|preview|migrate；前端 SettingsView 向导真实串六步（选择→检查与范围预览→确认迁移文本门禁→执行→重新打开提示），禁用占位移除
- 验证证据：后端 v21WorkspaceMigration 5 pass（活动任务阻断/嵌套阻断/范围预览/六步迁移含注释保留/配置写失败不落盘）；前端 v21DataTools 3 pass
- 下一任务：B 类向导（B1 更新分镜结构 diff）

## 2026-09-11｜R3-B1 进展（更新分镜结构 diff 向导）

- 状态：完成
- 实现方式：storyboardService 新增 previewStructureDiff（只读推导+分组对比，人工改动=structure_revision>1 标注）与 applyStructureDiff（事务内增/改/软删；跳过项不动；候选保留；受影响镜头 revision 递增）。路由 GET structure-diff / POST apply-structure-diff；前端更多菜单项改为向导 Modal（预览 diff 行 + 人工改动默认勾选跳过 + 确认创建新结构版本并刷新）
- 验证证据：后端 v21StructureDiff 4 pass（预览只读/removed 人工标注/跳过与候选保留/revision 递增与冲突保护），分镜回归 24 pass；前端 v21ProductionStudio 9 pass
- 下一任务：B2 小说拆集向导

## 2026-09-11｜R3-B2/B3 进展（小说拆集 + 来源视频向导）

- 状态：完成
- 实现方式：
  - B2：发现既有 /import-novel/confirm 只解析不落库——新增 `v21/import/novelSplitService.js`（preview：规则解析+字数+建议集号+冲突标注；confirm：逐集 createEpisode（冲突跳过给原因）+ saveDraft 剧本草稿，零媒体任务），路由切换至该服务；前端新建 NovelImportView（三步：粘贴/上传→章节预览→结果），路由 /projects/:id/episodes/import-novel，剧集页入口直达
  - B3：前端新建 SourceVideoView（三步：选择目标集（复用剧集列表+空白标注）→ 本地路径/URL+许可确认 → 登记并跳成片页），路由 /projects/:id/episodes/import-video，复用既有 /episodes/source-video 零生成登记；剧集页入口直达
- 验证证据：后端 v21NovelSplit 3 pass + v21Integration.e2e 全绿（含修复 routes.js 中 A5 编辑引入的重复声明块）；前端 v21ImportWizards 2 pass
- 下一任务：B4 人物音色真实来源

## 2026-09-11｜R3-B4/B5 进展（人物音色真实来源 + 新增状态）

- 状态：完成
- 实现方式：
  - B4：episode_asset_selections 增加 voice_json 列（幂等 ensure）；updateSelection 支持 voice（undefined=不变 / null=清除）并投影回读；前端音色抽屉——上传接 POST /api/v1/characters/:id/sd2-voice-upload、已认证音色读 seedance2_voice_asset、素材库音色接 GET /api/v1/character-library、audio 试听、提取入口保留并标注 P2；完成时 updateSelection 落 voice 指针
  - B5：新增状态 chip 接 POST /api/v1/characters/:id/variants（name 必填），创建后刷新 v21 详情投影；同视图图片 URL 输入改专用 Modal、进入分镜提示改 notice 条（原生弹窗清除）
- 验证证据：后端 v21VoicePointer 1 pass；前端 v21VoiceAndVariants 2 pass + v21ProductionStudio 回归 9 pass
- 下一任务：B6 自由创作加入资产库

## 2026-09-11｜R3-B6 进展（自由创作加入个人资产库）

- 状态：完成
- 实现方式：api 客户端新增 addToLibrary(kind, body)（复用 v1 character/scene/prop-library create 端点）；QuickCreateView 入库按钮真实调用（可选三类库，入库携带产物 URL/local_path/提示词摘要/source_type=quick-create+taskId 可追溯）；放弃确认改专用 Modal；原生弹窗与 comingSoon 占位清除
- 验证证据：前端 v21QuickCreate 1 pass
- 下一任务：C 类（C1 专用容器 / C3+C6 异步轮询与费用真实化 / C4 Shot Package / C5 AI 下拉 / C2 排版化）
